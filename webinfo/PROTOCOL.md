# Web直前情報の収集プロトコル（設計・未実施）

⚠ 2026-09-09 作成。**収集は開始していない。** D'・②・③ の後に着手する候補。
⚠ 項目リストは**要承認**。承認後に凍結し、以後追加・変更しない。

## 位置づけ（ユーザー指示・2026-09-09）

> Claude自身の予想が市場に勝てるかを検証するのではなく、**レース前のWeb情報を
> 決められた項目として記録し**、蓄積後に「どの情報が市場に対して有効だったか」を判定する。

🔑 **Claudeは収集装置であって予想家ではない。** Claudeの競馬判断が市場コンセンサスの
影響を受けていても構わない。有効性は後から市場との比較で統計的に処刑する。

⇒ これにより「Claudeが正しいはず」という仮説を一切置かずに済む。

## 🔴 前回のGate 0案は撤回する

私が提案した「30レースでClaudeの判定と市場人気の相関を測り、|r|>0.5なら打ち切り」は
**3点とも誤りだった**（ユーザー指摘）。

| 誤り | なぜ誤りか |
|---|---|
| 市場と相関が高い＝無価値、としていた | 🔴 **前走着順がまさに反例**。人気と強く相関するのに市場は11.3pt過大評価（F-3・今日の3.5年再現） |
| 相関が低い＝価値がある、としていた | 単なる逆張りでも相関は低い |
| N=30 で相関係数を判定しようとした | N=30 の相関の標準誤差は約0.19。0.5の閾値は区別できない |

**Gate 0 は「情報収集・記録プロトコルの実用性確認」に変更する。**

---

## 🔑 Claudeが集めるのは4項目だけ

ご提示のリストを既存資産に突き合わせた結果、大半はClaude不要と判明した。

### A. 既に特徴量として存在する（分析時に history.db から自動 join）

| 項目 | 既存 |
|---|---|
| 距離変更 | `f_dist_change` / `f_dist_vs_optimal` |
| コース変更 | コース適性14列 |
| 馬場変更 | `f_track_cond` 他4列 |
| 乗り替わり | `error_tags` の `jockey_switch_win` |
| 斤量・過去の馬体重推移 | `f_weight_load` 他5列 |
| ローテーション | `f_days_since_last` / `f_interval_score` |

🔴 **これらをClaudeに記録させない。** 二重計上になり比較が汚れる。

### B. 既に別経路で測定済み（期待値の上限が見えている）

| 項目 | 既存の測定 |
|---|---|
| 前走の不利（詰まり・位置取り・展開） | 2026-08-18: `corner_all`/`agari_rank` の代理変数で市場残差 **+0.87〜1.18pt・複勝回収74%止まり**。「出遅れ度外視」は **−0.30pt で有害** |

⚠ テキストで読む方が代理変数より精度が高い可能性はあるので**記録はする**。
ただし上限が +1.18pt 付近であることを承知の上で。

### C. 🔑 Webにしか無い（これがClaudeの本業）

| 項目 | 型 | 備考 |
|---|---|---|
| `oikiri_grade` | S / A / B / C / 不明 | メディアの追い切り評価 |
| `oikiri_note` | 生テキスト（要約せず原文） | 後から再分類できるように**原文を残す** |
| `stable_comment` | なし / 中立 / 強気 / 弱気 / 不明 | ⚠ **重賞中心。平場はほぼ無い見込み** |
| `condition_note` | なし / 好意的 / 否定的 / 不明 | 状態に関する記述 |

### D. 機械で取れるのに未取得（Claude不要・別件）

| 項目 | 状態 |
|---|---|
| **当日の馬体重・増減** | 🔴 JRA公式オッズページの見出しに存在するのに `_parse_odds` が抽出していない。⚠ ただし今日の地図で `f_weight_last_diff` は ρ=−0.41（形が持ち越さない） |

---

## 記録の器

**新システムを作らない。** `race_notes` の `phase='pre'` に③パドックと同居させる。

```
テーブル   race_notes  (date, race_id, horse_num, notes_data JSON)
キー       web_* 接頭辞
対象       軸(RL1) と AI上位3頭のみ（全頭は現実的でない）
```

⚠ `race_notes` は `ON CONFLICT DO UPDATE SET notes_data=excluded.notes_data` で
**JSON を丸ごと差し替える**。📝・🐎・web の3経路が同じ行を書くので、
保存前に既存値を読み、自分の担当外のキーを戻すこと（2026-09-02 と同じ defect）。

## 必ず一緒に記録するもの

| | なぜ |
|---|---|
| `captured_at` | 発走前であることの証明 |
| `source_urls` | 🔴 後から「結果ページを見ていないか」を第三者が検証できる |
| `n_found` | 🔑 **何項目見つかったか（0〜4）**。「情報が無かった」ことそのものがデータ |
| `model` | 収集したモデル（判定基準が変わりうるため） |

---

## Gate 0（30レース・3週末）— 実用性の確認だけ

Claudeが市場と違うことを言えるかは**測らない**。測るのは次の6点。

1. 発走前に情報を確定できるか
2. 取得時刻を保存できるか
3. 参照URLを保存できるか
4. 結果を見ずに記録できるか（**セッションを分ける**）
5. 🔑 **同じ情報を毎回同じ基準で分類できるか**
   → 同じ10レースを**別セッションで2回**判定し、一致率（Cohen's κ）を出す
6. 後から項目を追加・変更していないか（下記の凍結機構で担保）

### Gate 0 の主目的: 🔑 **充足率の実測**

| 項目 | 平場での充足率 | 重賞での充足率 |
|---|---|---|
| `oikiri_grade` | ？ | ？ |
| `stable_comment` | ？ | ？ |
| `condition_note` | ？ | ？ |

**先に決めておく処理**:
- 平場での充足率が **20%未満**の項目は、本収集の対象から外す（重賞限定項目として分離）
- 理由: 充足率20%なら N の蓄積が5倍遅くなり、1年が5年になる

⚠ Gate 0 で「そもそも情報が無い」と分かるのが**最も価値のある結果**。
その場合、この方向は本収集に入る前に打ち切れる。

## 項目凍結の担保（ご指摘6番目の技術的裏づけ）

「後から追加しない」を約束ではなく**機構**にする。

```
webinfo/FIELDS.json     項目リストと選択肢を定義。承認後にコミットしてハッシュを固定
保存コード              このリストに無いキーは保存時に拒否する
テスト                  未定義キーを渡すと落ちることを固定する
```

---

## 評価方法（蓄積後・事前登録）

🔴 **単純な好走率ではなく、同程度の市場人気と比べた増分**で測る（ユーザー指示）。
これは今日のミスプライシング地図と同じ手法（`forecaster/mispricing.py`）。

```
対象     項目 F が値 v の馬
対照     同じ市場人気帯 × 同じ頭数帯 の全馬
測る量   実測3着内率 − 市場示唆3着内率        （＝人気で説明できない分）
```

| 段階 | 内容 |
|---|---|
| 探索期 | 蓄積の前半。ここ**だけ**を見て「効いていそうな項目」を選ぶ |
| 確認期 | 蓄積の後半。選んだ項目**だけ**を検定する |
| 帰無 | 乱数列で同じ計算をして、偶然出るスプレッドの分布を出す（今日は 95%点3.2pt・最大4.7pt） |
| 🔴 ROI | **最後**。着順で再現してから初めて見る |

⚠ 交互作用（例「距離延長＋前走不利」）を見る場合も、**探索期で決めて確認期で検定**する。
探索期を見ずに交互作用を後から思いつくのは禁止。

---

## N と時間の見積り

軸＋上位3頭＝4頭 × 6R/日 × 2日 = **48頭/週** / レース単位 **12R/週**。

| 単位 | 必要N | 到達（充足率100%の場合） |
|---|---|---|
| 馬単位 | 2,000頭 | 約10ヶ月 |
| レース単位 | 300R | 約6ヶ月 |

⚠ 探索期/確認期に割るなら**倍**。充足率が50%ならさらに倍。
**現実的には1〜2年**。③パドック（土日両方で約5週）よりずっと遅い。

## 正直な期待値

✅ **設計としては健全になった。** Claudeを予想家として評価しないので、
   私の判断力への懸念（事前分布が群衆寄り）は論点から外れる。

⚠ ただし期待値は控えめに見ている:
- 追い切り評価は**日本で最も広く読まれている事前情報の1つ**。広く読まれているものが
  市場に織り込まれていない確率は低い
- ただし決定的ではない。**前走着順も universally known なのに市場は11.3pt過大評価している**。
  周知であることは正しい値付けを保証しない
- 平場での材料の薄さが最大のリスク。**Gate 0 がそこを最初に潰す**

## 触らないもの

- 本番の予想・モデル・買い目には繋がない（③と同じく記録のみ）
- D'・②・③ を先に終える
