# web_notes

週末の予想生成日に web から集めた情報の記録置き場（`YYYY-MM-DD.json`）。
スキーマは `data/web_note_schema.json`。

🔴 **ここの内容は予想に一切反映されない。** 反映するかどうかは、
溜まってから事前登録した基準で測って決める（パドック入力と同じ順序）。

実際に予想へ届く web 由来の情報は、いまのところ
**当日の馬場状態（`src/scraper/going.py`）だけ**で、
これは web の要約ではなく**その日すでに終わったレースの結果**から取っている。
