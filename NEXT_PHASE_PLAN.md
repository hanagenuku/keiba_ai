# NEXT_PHASE_PLAN — Phase 1〜10

設計指示書 §20 C への回答。各 Phase に
**仮説 / 使用データ / OOS期間 / 評価指標 / 採用基準 / 中止基準**を書く。
前提の棚卸しは `HORSE_ABILITY_DESIGN.md` / `COURSE_ABILITY_MAP.md`。

**設計レビューが終わるまで本番モデルを書き換えない**（§20）。現時点で Phase 1 のみ完了。

---

## 0. 先に、この計画の見込みを正直に書く

指示書 §19 の到達点（「総合能力ではメンバー上位だが、今回の中山ダート1800mでは
能力を発揮しにくい」と言えるAI）は、**部分的に4回測られていて、4回とも AUC では届いていない**。

| 測ったもの | 結果 |
|---|---|
| コース適性に専用の学習枠を与える2段階モデル（2026-08-04） | **-0.0025** |
| 道悪 × 馬の型（スピード/スタミナ）の交互作用5列（2026-09-02） | **-0.0008**（道悪レースだけに絞っても -0.0007） |
| 完璧なペース／位置取りを与えた場合の天井（2026-08-27） | ペース **+0.0007** / 事前予測できる位置取り **+0.0001** |
| 会場one-hot を本体に足す（2026-09-15②） | **-0.0003** |

🔑 4回とも同じ機序に着地している——
**交互作用は実在するが、木は元からそれを作れる状態にあり、明示的に手渡しても上乗せがゼロ。**

したがってこの計画は「効くはずだからやる」ではなく、
**「どこまでなら違う結果になりうるか」を1つずつ潰す**設計にする。
指示書 §17② のとおり、既に否定された方向は**何が今回違うのかを1行で言えなければ着手しない**（§12）。

⚠ 指示書 §17③ は「AUC だけで成功判定しない、最終評価は実オッズ×OOS×ROI」と定める。
これは守る。ただし本プロジェクトの実測では**AUC が動かないものは回収率も動かない**
（2026-08-18・08-24・08-31）。**AUC を目的にはしないが、AUC が動かないものを
回収率で救おうとはしない**（ROI は分散が大きく、小さいNなら偶然100%を超える）。

---

## 1. 全 Phase に共通のルール

| | |
|---|---|
| 事前登録 | 数字を見る**前**に採用・中止基準を `noryoku/CRITERIA_PHASE<N>.md` に書く。書いていない指標を後から採用根拠にしない（2026-08-28 R1 で一度踏んだ） |
| 感度検査 | 本データの前に**既知の効果を注入して検出できるか**を確かめる。通らなければ本データを読まずに中止（2026-09-15③ で有効だった） |
| 対照群 | プラセボ（ラベルのシャッフル）で安心しない。「本物があっても0になる設計か」を別に問う（North Star #11） |
| 計測器の検算 | 新しい量は**本番パイプラインで**作り、別実装と値を突き合わせる（North Star #8） |
| ハーネス | 本番同型（134列・residual・**ドリフト注入ON**・同一の行順）。A/B は同じハーネスで回す |
| 探索と確認の分離 | セルを切って最大値を拾わない（§17④）。選抜期と検証期を必ず分ける |
| **効果の分散** | 全体の改善が**上位1〜2の単位（組・会場・セグメント）に集中していないか**を必ず出す。良くなった単位の数・中央値・四分位を並べる。2026-09-16 に、P=0.010 を通った効果が**1組を抜くと消えた**（`tenkai/g4.py`）。**Pは分散を保証しない** |
| **上位依存性** | 寄与の大きい単位を1つ・2つ・3つと**抜いて測り直す**。1つ抜いて消えるものは一般的な効果ではない |
| **年跨ぎ** | 同じ年の中で割らない。前期で選び後期で測り、**順位が持ち越すか**（ρ・符号の一致）まで出す。2026-08-24（Δの順位 r=-0.001）・2026-09-15②（前期上位セルが後期で符号反転）が実例 |
| 混ぜない | 馬能力DB / コースDB / メンバー / Web情報を別管理し、掛け合わせは最後（§17⑤） |

### 能力軸の採否（2026-09-16 のレビューで確定）

🔑 **AUC が上がらない＝その能力軸に価値がない、ではない。**
本計画の目的は「今回の条件で能力を発揮できるか」を**説明できること**でもあるため、
AUC が動かなくても**語彙として残してよい**（Phase 3・4 が使う）。

🔑 **逆も成り立つ。統計的に効いていても、効果が1〜2条件に集中しているなら、
それを一般的な能力軸として扱わない。**「中山ダート1200ではこうなる」という
**条件固有の観察**として記録し、軸には昇格させない。

| 判定 | AUCゲート | 効果の分散 | 扱い |
|---|---|---|---|
| 一般的な能力軸 | 通る | 分散している | 軸として採用。Phase 3・4 で使う |
| 説明用の語彙 | 通らない | — | 軸として残すが**本番モデルには入れない**。表示・説明にのみ使う |
| 条件固有の観察 | 通る | 1〜2条件に集中 | **軸にしない**。条件付きの記録として残す |
| 不採用 | 通らない | 集中 | 捨てる |

### AUCゲート（情報があるか）
1. 3窓すべてでプラス
2. 3窓平均 **+0.002 以上**
3. シード std の **2倍以上**
4. Brier / LogLoss も同方向

**OOS窓（本番と同じ3窓を固定して使う）**
W1 2026-07-04〜08-16 ／ W2 2026-05-02〜06-28 ／ W3 2026-03-01〜04-26、各3シード。

### 市場比較ゲート（§14・AUCゲートを通ったものだけ）

🔑 **一発判定にしない**（2026-09-16 のレビューで確定）。
「AIが市場に勝ったか」ではなく、**3者比較＋乖離の方向別**で見る。

5. **AI単独 / 市場単独 / AI×市場** の3本を同じ窓・同じ行で比較する
   - AI単独 … `ability_margin`（市場に依らないAI評価）だけで順位付け
   - 市場単独 … 人気順位（または実オッズ）だけ
   - AI×市場 … 本番と同じ残差学習
6. **乖離の方向別**に分けて評価する（まとめて平均しない）
   - AIが市場より**強気**な馬（`gap = 人気順位 − AI順位` が正）
   - AIが市場より**弱気**な馬（`gap` が負）
   - 一致している馬
   を別々に、複勝率・市場残差・回収率で出す

> ⚠ 6 は過去に**逆の結果**が3回出ている。AIが市場より強気なほど実勝率・回収率が
> **単調に下がる**（2026-07-30 / 08-03 / 08-31）。さらに 2026-08-31 には
> `corr(gap, 符号つき誤差) = -0.1213` ＝**市場と食い違うほど、その方向にAIが
> 間違っている**と実測されている。
> 🔑 だから「AIが強気な側で改善した」が出たら、**まず計測器を疑う**。
> 逆に「AIが弱気な側（市場が買いすぎ）で当たる」なら、それは過去の測定と
> 同じ向きなので、より信用してよい。

### 経済ゲート（§17③・上2つを通ったものだけ）
7. OOS 1,500レース以上・**実オッズ・実配当**
8. 日ブロック bootstrap の **95%CI 下限が 100% を超える**
9. 上位3本の払戻を抜いても 100% を保つ
10. 前半・後半に割っても符号が変わらない

> 🔑 7〜10 を満たしたものは**まだ一度も無い**。最良は「高自信レースの軸・単勝
> 95.9%（N=190・21開催日）」で、大規模では 93.4% が天井（2026-08-18③）。

---

## Phase 1 — 現在の馬特徴量の棚卸し ✅ **完了**

| | |
|---|---|
| 仮説 | 134列には「馬能力 / 市場由来 / レース由来 / コース由来」が混在し、名前からは判別できない |
| 使用データ | `history.db` 2026年の実レース8つ＋本番モデルの gain |
| 方法 | 本番と同じ経路を回し、**入力を1つずつ摂動**して依存を測る（`noryoku/inv1.py`） |
| 結果 | レース共通10列 4.3% / 馬×今回の条件45列 33.2% / 馬のみ79列 62.5%。**今回の市場に依存する列は0**。`f_course_*` は3種類の混合だった。死んだ列が4つ |
| 判定 | ✅ 完了。詳細は `HORSE_ABILITY_DESIGN.md` |

### Phase 1b — 土台の掃除（Phase 2 の測定可能性のため・モデル精度は上げない）

| | |
|---|---|
| 仮説 | 死んだ列・重複列が残っていると「軸を足した効果」を測れない |
| 使用データ | `build_training_data()` で再生成した全期間CSV（15分） |
| OOS期間 | W1〜W3 |
| 評価指標 | AUC（**落ちないこと**の確認） |
| 採用基準 | 4列（`f_early_speed` / `f_is_debut` / `f_same_course_rate` / `f_course_stamina_demand`）を削って **3窓とも -0.001 以内** |
| 中止基準 | `f_early_speed` が学習データで定数でない → 棚卸しが誤り。やり直す |

---

## Phase 2 — 馬能力を複数の能力軸に分解する

| | |
|---|---|
| 仮説 | 馬側79列は、人が名前で付けた分類ではなく**データから出る少数の因子**で説明でき、その因子は元の列より良い表現になる |
| 使用データ | Phase 1b のCSV（馬側の列のみ。市場3列とレース共通10列は**入れない**＝§17⑤） |
| 方法 | 因子分析／PCA。**因子数は固有値と交差検証で決め、手で決めない**（§6 Step 2「型を人間が勝手に決めない」）。因子スコアを特徴量にして本番同型A/B |
| OOS期間 | W1〜W3 × 3シード |
| 評価指標 | AUC / Brier / LogLoss、および因子の解釈可能性（指示書 §3 A〜E の軸と対応がつくか） |
| **採用基準（本番モデルに足すか）** | AUCゲート 1〜4 |
| **中止基準** | **因子が2つ以下**（＝実質1軸で、分解する意味がない）→ この筋を閉じる |
| 🔑 AUC未達の扱い | **AUCが上がらない＝能力分解に価値がない、ではない**（2026-09-16 のレビューで確定）。AUC未達なら**因子を本番モデルに足さない**だけで、因子は Phase 3・4 が使う**語彙**として残す。Phase 3・4 はそれぞれ自分のゲートで判定する |
| ⚠ 歯止め | ただし無条件に先へ進めない。**Phase 3 の持ち越し基準**が通らなければ Phase 4 に進まない。**Phase 4 が AUCゲート未達ならこの筋を閉じる**（＝どこかで必ず止まる設計にする） |
| 既知 | 手で切った7軸では PCA 第1主成分 **39%**、軸間相関の中央値 0.27、**脚質は他軸とほぼ無相関**。1軸ではない。ただし軸内の重複が大きい（`f_speed_fig_avg × max` = +0.93） |
| 見込み | 🟡 軸が出ること自体は期待できる。**AUC が動く見込みは低い**（木は元の列を全部見ており、因子は言い換えになりやすい）。それでも Phase 3・4 の入口としては要る |

---

## Phase 3 — コースDBを能力軸と同じ言葉で表現する

> ✅ **要求度は「実測から作る」側だけを残す**（2026-09-16 のレビューで確定）。
> 人が物理量から式を書く形は `f_course_stamina_demand`（gain **0.00%**）で
> 一度検証済みで、別の式にして掘り直すと恣意的な特徴量作りに入る。


| | |
|---|---|
| 仮説 | コースの物理量から「このコースが要求する能力プロファイル」を作れる。指示書 §6 Step 3 の形（馬 持続力0.8 × コース 持続力要求度0.7） |
| 使用データ | `data/course_starts.json`（127組・実レースの98.8%）＋ `race_history` |
| 方法 | 要求度は**勝手に定義しない**。実測から作る——そのコースで実際に好走した馬の Phase 2 因子スコアの平均を「要求度」とする（リーク防止: 対象レースより前のみ） |
| OOS期間 | 要求度の推定期間と評価期間を分ける。W1〜W3 |
| 評価指標 | ① 要求度が**年をまたいで持ち越すか**（前期→後期の相関） ② 未見コースへ外挿できるか |
| **採用基準** | ① 持ち越しの相関が **正・P<0.05**、かつ ② シャッフル対照群の相関が0付近 |
| **中止基準** | 持ち越さない（2026-09-15② の市場残差オフセットと同じ壊れ方）→ Phase 4 に進まない |
| 既知 | ✅ **ペースについては持ち越す**（前半600mのコース定数 ρ=+0.819, P=0.0005）。🔴 **市場残差については持ち越さない**（ρ=+0.158, P=0.20・上位セルは符号反転）。つまり**コースは走りを決めるが、市場はそれを完全に値付けしている** |
| 🔴 既知（2026-09-16 追加） | **未見コースへの外挿は成立しない**。leave-one-course-out の改善 -0.0153s は**1組（中山ダート1200）がほぼ全部**で、抜くと -0.0015s。**会場ごと未見にすると +0.0021s＝改善なし**。⚠ シャッフルの P=0.010 は通る——**Pは「正しい組に当てているか」しか見ておらず、1組が担いでいても通る**。この Phase の基準に**集中度の検査（上位1〜2組を抜いても残るか）を必ず入れる** |
| 見込み | 🟡 ①は通る可能性がある。ただし②（市場に残るか）は既に否定側。外挿も否定側 |

---

## Phase 4 — 能力 × コース適性の相互作用【今回のメイン】

| | |
|---|---|
| 仮説 | Phase 2 の能力軸 × Phase 3 の要求度を**明示的に**渡すと、木が自力で作るより良い。「総合能力1位だが今回は下げる」が数字で出る |
| 使用データ | Phase 1b のCSV + Phase 2 因子 + Phase 3 要求度 |
| 方法 | 交互作用は**5列以内**（69組・134列という規模に対し列を増やすと外挿が壊れる。2026-09-15③ で実測） |
| OOS期間 | W1〜W3 × 3シード |
| 評価指標 | AUC / Brier / LogLoss、**さらに「能力順位と今回期待順位が入れ替わった馬」だけに絞った成績** |
| **採用基準** | AUCゲート 1〜4 ＋ 市場比較ゲート 5〜6 |
| **中止基準** | ① Phase 2 が「因子2つ以下」で中止、または Phase 3 の持ち越しが通らない ② 3窓平均 +0.002 未満 ③ **条件を満たすレースだけに絞っても**改善しない（2026-09-02 の道悪と同じ形）。→ **②③ のいずれかなら、能力×コース適性という筋そのものを閉じる**（Phase 2 の「構造化としては残す」はここまで） |
| 既知 | 🔴 同型が3回とも -0.0003〜-0.0025。掛け算の形（`f_course_fit_score`）は**既に実装済み**で gain 0.55%、要求度の片側（`f_course_stamina_demand`）は**木が一度も使っていない** |
| 見込み | 🔴 低い。前回との差分は「軸をデータ駆動で作った点」だけ |

---

## Phase 5 — メンバーレベル（絶対能力と相対能力を分ける）

| | |
|---|---|
| 仮説 | §9 のとおり「絶対能力」と「今回のメンバー内での相対能力」は別の量で、分けた方が良い |
| 🔑 現状 | **相対化はもう入っている。しかも最大の塊**。`rl_*` / `cl_*` の **43列が gain の 38.0%** を占める |
| 🔴 既知の事故 | `f_member_level_*`（前走のメンバーレベル）は **2026-07-28 に未来参照リークで削除**。相手馬の**そのレース以降**の成績を使っていた（回収率261%の正体） |
| 使用データ | `history.db`。**予測日より前の相手成績のみ**（線引き: 予測日より後＝リーク／過去走から予測日まで＝合法） |
| OOS期間 | W1〜W3 |
| 評価指標 | AUC。加えて**能力差が大きいレース**に絞った的中率 |
| **採用基準** | AUCゲート 1〜4。さらに**リーク検査**——初出走馬の値が定数、同日内で不変、別実装との値の突合 |
| **中止基準** | ① リーク検査に1つでも落ちたら即中止（過去に一度これで撤回している） ② 3窓平均 +0.002 未満 |
| 見込み | 🟡 相対化が既に 38% を占めるため、追加の取り分は薄い。⚠ ただし「絶対 vs 相対」の**分離した表示**（§15）は Phase 8b で別に価値がある |

---

## Phase 6 — ペースモデルをレース記述として接続する（本体AUCは狙わない）

| | |
|---|---|
| 仮説 | ペースモデルは**レースの説明**として有用。本体精度の向上には使わない（§8 の天井を尊重） |
| 現状 | 前半600m OOS RMSE 0.8468s / P(先頭) AUC 0.7805。入力は**レース条件だけ**（距離・表面・クラス・頭数）で学習/推論の入力が構造的に同一 |
| 使用データ | `race_shape_model.pkl` |
| 評価指標 | ペース予測自体の RMSE のみ |
| **採用基準** | 表示・説明への接続のみ。**本体モデルの予測値を1ミリも変えないこと** |
| **中止基準** | 予測値・買い目が変わったら差し戻す |
| 既知 | 完璧なペースでも本体 +0.0007、展開系29列を全部抜いても -0.0001 |

---

## Phase 7 — Web情報の取得機構

| | |
|---|---|
| 🔴 制約 | この環境は egress proxy で**個別ページに到達できない**（`jra.go.jp` も `radionikkei.jp` も実測で EGRESS_BLOCKED）。使えるのは WebSearch の**要約**だけで、要約は複数の日付が混ざる |
| 🔴 制約 | **過去分が存在しない**ため学習データが作れない（North Star #3）。§11 の「後から結果を見て記事を取得しない」は自動的に守られる |
| 現状 | 層1（当日の馬場＝その日すでに終わったレースの結果から取得）は予想に接続済み。層2（webの記録）は `data/web_notes/` に**記録のみ**（`model_consumed: false` をスキーマが強制） |
| **Gate 1（§13）** | 収集可能性とカバレッジ。**週あたり何レース・何頭ぶん取れたか**を毎週記録する |
| **中止基準** | 3ヶ月続けて `official` のカバレッジが出走頭数の 10% 未満なら、この経路は打ち切って**パドック入力（ユーザー手入力）に一本化**する |

---

## Phase 8 — Web情報の構造化

| | |
|---|---|
| 現状のスキーマ | `data/web_note_schema.json` は **会場単位（馬場・天候・内柵）とレース単位（取消・乗替）**しか持たない |
| 🔴 ギャップ | §11 が求める**馬単位**の行（`horse_id, race_id, source, timestamp, field, value, confidence`）が**無い**。調教・陣営コメント・ローテは全部この単位 |
| やること | 馬単位の行を追加する。`field` は列挙（`training_time` / `training_note` / `comment` / `rotation` / `body_weight` / `paddock`）、`value` は文字列＋数値の両持ち |
| **§12 の分離** | `evidence_type` を必須にする: `stated`（陣営が明言した）/ `observed`（数字がある）/ `inferred`（Claudeの推測）。**`inferred` は特徴量候補にしない**。「今回は叩き」は原則 `inferred` |
| **採用基準** | 検証関数（`validate_web_note`）が `source` と `confidence` と `evidence_type` の無い行を**落とすこと**をテストで固定 |
| **中止基準** | なし（記録のスキーマなので、予想に触れない限り危険がない） |

### Phase 8b — 表示の分離（§15・独立して進めてよい）

| | |
|---|---|
| 目的 | 「強い馬」と「今回向いている馬」を別表示する。総合能力 / コース適性 / 今回条件適性 / メンバー比較 / Web情報 / 総合期待 |
| 現状 | `shap_explain.py` が `ability_margin` を15カテゴリに分解済み（画面に📊で表示） |
| やること | カテゴリを `HORSE_ABILITY_DESIGN.md` の仕分け（馬能力 / 今回の条件 / 掛け合わせ / 過去の市場）に**貼り替える**。表示のみ |
| **採用基準** | 「なぜこの馬を下げたか」が1行で読めること。**予測値を変えないこと** |
| **中止基準** | 予測値・買い目が変わったら差し戻す |
| 🔴 既知 | **SHAPの内訳から「AIを信じてよいか」は読めない**（-0.0038・2026-08-27）。説明であって選別ではない |

---

## Phase 9 — Web情報による最終補正を OOS で検証

| | |
|---|---|
| 仮説 | 調教・陣営・ローテには、市場が織り込んでいない情報が残っている |
| 使用データ | Phase 7〜8 で溜めた `official` / `observed` の行のみ |
| **Gate 3** | 過去データで同じ情報が取れるか（取れなければ学習データが作れない） |
| **Gate 4** | 情報ごとの**単独効果**。1つずつ測る |
| **Gate 5** | 市場との重複（市場残差に対する寄与） |
| **Gate 6** | OOS での再現性（前半→後半で符号が変わらない） |
| 必要N | **1,000頭以上**（パドック入力と同じ基準。週168頭で約3ヶ月） |
| **採用基準** | AUCゲート 1〜4 ＋ 市場比較ゲート 5〜6 |
| **中止基準** | ① N が基準に届かないうちは**特徴量化しない** ② 単独効果が全項目でヌル |
| 🔴 絶対禁止 | **後付けの補正レイヤーにしない。** 本プロジェクトで予想の出力を後から書き換える層は例外なく撤回されている（市場補正レイヤー / error_tags週次補正 / rank_matrix_filter / AI xx%バッジ / ROI予測150%）。入れるなら**上流（特徴量）に入れてA/Bを組む** |

---

## Phase 10 — 市場との比較と実オッズでの経済評価

| | |
|---|---|
| 対象 | Phase 2〜5・9 のうち、AUCゲートと市場比較ゲートを通ったものだけ |
| 使用データ | 実オッズ（netkeiba 53,916頭が artifact にある）＋ `race_dividends` の実配当 |
| OOS期間 | 2025-07-05〜2026-08-23（3,969レース・125開催日） |
| 評価指標 | 回収率、日ブロック bootstrap の95%CI、3本抜き、前後半 |
| **採用基準** | 経済ゲート 7〜10 |
| **中止基準** | CI下限が100%を超えない → **実装しない**（2026-07-27 の「基準未達でも入れる」で一度痛い目を見ている） |
| 見込み | 🔴 ここを通ったものはまだ無い。**通らないのが最も起こりうる結末**であり、先に書いておく |

---

## 依存関係と打ち切り

```
Phase 1 ✅──→ Phase 1b ──→ Phase 2 ──→ Phase 3 ──→ Phase 4 ──→ Phase 10
                               │     AUC未達でも      持ち越さ    ここが最終関門。
                               │     語彙として先へ    なければ止  未達ならこの筋を閉じる
                               │     （因子2つ以下なら止）  める
                               └──→ Phase 5（メンバー）

Phase 6（ペース＝記述）      … 独立。本体AUCは狙わない
Phase 7 → 8 → 9（Web情報）  … 時間が解決するまで記録のみ
Phase 8b（表示の分離）       … 独立。予測値を変えない
```

**止まる場所は3つある**（2026-09-16 のレビューで確定）。
① Phase 2 で因子が2つ以下 → この筋を閉じる
② Phase 3 で要求度が年をまたいで持ち越さない → Phase 4 に進まない
③ Phase 4 が AUCゲート未達 → 能力×コース適性という筋を閉じる

⚠ **Phase 2 の AUC 未達だけでは止めない。** 能力分解は Phase 3・4 の語彙として
意味があるため（§19 の「総合能力1位だが今回は下げる」を言うには軸が要る）。
ただし ③ まで来て未達なら、そこで閉じる。

止まった場合に残るのは Phase 1b（掃除）・6（記述）・8b（表示）・7〜9（蓄積）で、
**いずれも回収率を上げない**。それがこの計画の最も起こりうる結末である。

---

## 再提案しないこと（§17②）

- 展開・隊列の精緻化／シミュレーション（三連複 +0.35%・必要 +33.3%）
- ペース × 脚質の細分化（完璧なペースでも +0.0007）
- コース one-hot・コース物理量の**本体投入**（-0.0003 / -0.0025）
- 市場アンカーを外す・弱める（3回否定）
- 人気帯・オッズ帯・乖離・主因・券種・セグメントでの**絞り込み**
- 資金配分・掛け金の最適化（総期待値は EV の加重平均。算数で動かない）
- EV = AI確率 × オッズ を買い目の根拠にする（3回否定・本番8,546頭で回収 57.9%）
- 特徴量を増やすこと自体（129特徴量 vs 市場アンカーのみ ＝ **+0.0009**）

**着手の条件**：上に触れる提案は「前回と**データ・機序・測り方のどれが**変わったか」を
1行で書けること。書けないなら着手しない。

---

## 最初の1歩（レビュー後に着手する順）

1. **Phase 1b** を回す（`build_training_data` 15分 → 死んだ列・重複列の確認）
2. `noryoku/CRITERIA_PHASE2.md` に Phase 2 の採用・中止基準を書く
3. **感度検査**（既知の因子構造を注入して検出できるか）を本データの前に通す
4. Phase 2 を測る

⚠ 1〜4 のどこでも、**本番モデル・買い目ロジック・表示は変更しない。**
