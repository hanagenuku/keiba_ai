# NEXT_PHASE_PLAN — Phase 1〜10

設計指示書 §20 C への回答。各 Phase に
**仮説 / 使用データ / OOS期間 / 評価指標 / 採用基準 / 中止基準**を書く。
前提の棚卸しは `HORSE_ABILITY_DESIGN.md` / `COURSE_ABILITY_MAP.md`。

**設計レビューが終わるまで本番モデルを書き換えない**（§20）。現時点で Phase 1 のみ完了。

---

## 0. 先に、この計画の見込みを正直に書く

指示書 §19 の到達点（「総合能力ではメンバー上位だが、今回の中山ダート1800mでは
能力を発揮しにくい」と言えるAI）は、**部分的に4回測られていて、4回とも AUC では届いていない**。

| 測ったもの | 結果 |
|---|---|
| コース適性に専用の学習枠を与える2段階モデル（2026-08-04） | **-0.0025** |
| 道悪 × 馬の型（スピード/スタミナ）の交互作用5列（2026-09-02） | **-0.0008**（道悪レースだけに絞っても -0.0007） |
| 完璧なペース／位置取りを与えた場合の天井（2026-08-27） | ペース **+0.0007** / 事前予測できる位置取り **+0.0001** |
| 会場one-hot を本体に足す（2026-09-15②） | **-0.0003** |

🔑 4回とも同じ機序に着地している——
**交互作用は実在するが、木は元からそれを作れる状態にあり、明示的に手渡しても上乗せがゼロ。**

したがってこの計画は「効くはずだからやる」ではなく、
**「どこまでなら違う結果になりうるか」を1つずつ潰す**設計にする。
指示書 §17② のとおり、既に否定された方向は**何が今回違うのかを1行で言えなければ着手しない**（§12）。

⚠ 指示書 §17③ は「AUC だけで成功判定しない、最終評価は実オッズ×OOS×ROI」と定める。
これは守る。ただし本プロジェクトの実測では**AUC が動かないものは回収率も動かない**
（2026-08-18・08-24・08-31）。**AUC を目的にはしないが、AUC が動かないものを
回収率で救おうとはしない**（ROI は分散が大きく、小さいNなら偶然100%を超える）。

---

## 1. 全 Phase に共通のルール

| | |
|---|---|
| 事前登録 | 数字を見る**前**に採用・中止基準を `noryoku/CRITERIA_PHASE<N>.md` に書く。書いていない指標を後から採用根拠にしない（2026-08-28 R1 で一度踏んだ） |
| 感度検査 | 本データの前に**既知の効果を注入して検出できるか**を確かめる。通らなければ本データを読まずに中止（2026-09-15③ で有効だった） |
| 対照群 | プラセボ（ラベルのシャッフル）で安心しない。「本物があっても0になる設計か」を別に問う（North Star #11） |
| 計測器の検算 | 新しい量は**本番パイプラインで**作り、別実装と値を突き合わせる（North Star #8） |
| ハーネス | 本番同型（134列・residual・**ドリフト注入ON**・同一の行順）。A/B は同じハーネスで回す |
| 探索と確認の分離 | セルを切って最大値を拾わない（§17④）。選抜期と検証期を必ず分ける |
| **効果の分散** | 全体の改善が**上位1〜2の単位（組・会場・セグメント）に集中していないか**を必ず出す。良くなった単位の数・中央値・四分位を並べる。2026-09-16 に、P=0.010 を通った効果が**1組を抜くと消えた**（`tenkai/g4.py`）。**Pは分散を保証しない** |
| **上位依存性** | 寄与の大きい単位を1つ・2つ・3つと**抜いて測り直す**。1つ抜いて消えるものは一般的な効果ではない |
| **年跨ぎ** | 同じ年の中で割らない。前期で選び後期で測り、**順位が持ち越すか**（ρ・符号の一致）まで出す。2026-08-24（Δの順位 r=-0.001）・2026-09-15②（前期上位セルが後期で符号反転）が実例 |
| 混ぜない | 馬能力DB / コースDB / メンバー / Web情報を別管理し、掛け合わせは最後（§17⑤） |

### 🔴 事前登録カードを書くときのチェックリスト（2026-09-22 に追加）

`betting/CRITERIA_STEP1.md` が下の3つ目を**書き忘れ**、測定が終わってから当てる羽目になった
（主効果 +8.88/+9.68pt のうち **67〜81% が市場評価の言い換え**と後から判明。
全文は `betting/RESULTS_STEP1.md`）。カードを書いたら、出す前にこの順で確認する。

| # | 項目 | 落とすと何が起きるか |
|---|---|---|
| 1 | **対照群A＝並べ替え分布**（`\|mean\| < 2*std/sqrt(n)`）| 偽の相関に気づけない |
| 2 | **対照群B＝注入の復元**（`注入後 − 注入前 = 注入量`）| 「本物があっても0になる設計」に気づけない |
| 3 | 🔴 **市場比較ゲート**（上記「市場比較ゲート」＝ AI単独 / 市場単独 / **AI×市場**）| **市場の言い換えを「新しい情報」と誤読する**。この項目は**全カード必須**。Betting Policy のように「市場を選択に使わない」研究でも、**情報が市場と独立かの確認は別に要る** |
| 4 | 閾値が**統計量自身の SE より大きい**か | SE より小さい閾値は「厳しい基準」ではなく「何も判定できない基準」（North Star #11） |
| 5 | 主張ごとの**測定器**を明記（入れる/抜くのアーム比較か、残差か）| 2026-09-17 `f_last1_rank` と同型の誤りになる |
| 6 | 効果の**分散**と**上位依存性**を出す約束 | 1単位が全部を担いでいても P は通る（2026-09-16） |
| 7 | 落ちた場合に使う**文**を先に書く | 結果を見てから目的を差し替えてしまう |

🔑 3 を「この研究は市場を見ない設計だから不要」と判断しない。
**「市場を選択に使わない」と「市場と独立な情報か確かめない」は別物。**

### 能力軸の採否（2026-09-16 のレビューで確定）

🔑 **AUC が上がらない＝その能力軸に価値がない、ではない。**
本計画の目的は「今回の条件で能力を発揮できるか」を**説明できること**でもあるため、
AUC が動かなくても**語彙として残してよい**（Phase 3・4 が使う）。

🔑 **逆も成り立つ。統計的に効いていても、効果が1〜2条件に集中しているなら、
それを一般的な能力軸として扱わない。**「中山ダート1200ではこうなる」という
**条件固有の観察**として記録し、軸には昇格させない。

| 判定 | AUCゲート | 効果の分散 | 扱い |
|---|---|---|---|
| 一般的な能力軸 | 通る | 分散している | 軸として採用。Phase 3・4 で使う |
| 説明用の語彙 | 通らない | — | 軸として残すが**本番モデルには入れない**。表示・説明にのみ使う |
| 条件固有の観察 | 通る | 1〜2条件に集中 | **軸にしない**。条件付きの記録として残す |
| 不採用 | 通らない | 集中 | 捨てる |

### AUCゲート（情報があるか）
1. 3窓すべてでプラス
2. 3窓平均 **+0.002 以上**
3. シード std の **2倍以上**
4. Brier / LogLoss も同方向

**OOS窓（本番と同じ3窓を固定して使う）**
W1 2026-07-04〜08-16 ／ W2 2026-05-02〜06-28 ／ W3 2026-03-01〜04-26、各3シード。

### 市場比較ゲート（§14・AUCゲートを通ったものだけ）

🔑 **一発判定にしない**（2026-09-16 のレビューで確定）。
「AIが市場に勝ったか」ではなく、**3者比較＋乖離の方向別**で見る。

5. **AI単独 / 市場単独 / AI×市場** の3本を同じ窓・同じ行で比較する
   - AI単独 … `ability_margin`（市場に依らないAI評価）だけで順位付け
   - 市場単独 … 人気順位（または実オッズ）だけ
   - AI×市場 … 本番と同じ残差学習
6. **乖離の方向別**に分けて評価する（まとめて平均しない）
   - AIが市場より**強気**な馬（`gap = 人気順位 − AI順位` が正）
   - AIが市場より**弱気**な馬（`gap` が負）
   - 一致している馬
   を別々に、複勝率・市場残差・回収率で出す

> ⚠ 6 は過去に**逆の結果**が3回出ている。AIが市場より強気なほど実勝率・回収率が
> **単調に下がる**（2026-07-30 / 08-03 / 08-31）。さらに 2026-08-31 には
> `corr(gap, 符号つき誤差) = -0.1213` ＝**市場と食い違うほど、その方向にAIが
> 間違っている**と実測されている。
> 🔑 だから「AIが強気な側で改善した」が出たら、**まず計測器を疑う**。
> 逆に「AIが弱気な側（市場が買いすぎ）で当たる」なら、それは過去の測定と
> 同じ向きなので、より信用してよい。

### 経済ゲート（§17③・上2つを通ったものだけ）
7. OOS 1,500レース以上・**実オッズ・実配当**
8. 日ブロック bootstrap の **95%CI 下限が 100% を超える**
9. 上位3本の払戻を抜いても 100% を保つ
10. 前半・後半に割っても符号が変わらない

> 🔑 7〜10 を満たしたものは**まだ一度も無い**。最良は「高自信レースの軸・単勝
> 95.9%（N=190・21開催日）」で、大規模では 93.4% が天井（2026-08-18③）。

---

## Phase 1 — 現在の馬特徴量の棚卸し ✅ **完了**

| | |
|---|---|
| 仮説 | 134列には「馬能力 / 市場由来 / レース由来 / コース由来」が混在し、名前からは判別できない |
| 使用データ | `history.db` 2026年の実レース8つ＋本番モデルの gain |
| 方法 | 本番と同じ経路を回し、**入力を1つずつ摂動**して依存を測る（`noryoku/inv1.py`） |
| 結果 | レース共通10列 4.3% / 馬×今回の条件45列 33.2% / 馬のみ79列 62.5%。**今回の市場に依存する列は0**。`f_course_*` は3種類の混合だった。死んだ列が4つ |
| 判定 | ✅ 完了。詳細は `HORSE_ABILITY_DESIGN.md` |

### Phase 1b — 土台の掃除（Phase 2 の測定可能性のため・モデル精度は上げない）

| | |
|---|---|
| 仮説 | 死んだ列・重複列が残っていると「軸を足した効果」を測れない |
| 使用データ | `build_training_data()` で再生成した全期間CSV（15分） |
| OOS期間 | W1〜W3 |
| 評価指標 | AUC（**落ちないこと**の確認） |
| 採用基準 | 4列（`f_early_speed` / `f_is_debut` / `f_same_course_rate` / `f_course_stamina_demand`）を削って **3窓とも -0.001 以内** |
| 中止基準 | `f_early_speed` が学習データで定数でない → 棚卸しが誤り。やり直す |

---

## Phase 2 — 馬能力を複数の能力軸に分解する

> ✅ **2026-09-16 に実施・完了した。結果は `noryoku/RESULTS_PHASE2.md`。**
> 設計は `noryoku/CRITERIA_PHASE2.md`（結果を見る前に確定）。
>
> | 問い | 答え |
> |---|---|
> | 最大gain群（8列・16.19%）は1軸か | ✅ **1軸**。合成1列にまとめて **ΔAUC −0.0000**（3窓・シードsd 0.0007）|
> | `x` と `x_vs_field`（17対）は両方要るか | ❌ **片方で足りる**。どちらを落としても **−0.0001** |
> | 独立した塊はいくつか | **解釈できるブロックは8個**。年跨ぎで **7/8 が持ち越す**（ρ 0.82〜0.99）|
> | 予測寄与では何軸か | **多軸構造は否定されない。ただし予測上の追加寄与の大部分が1ブロックに集中し、残りは薄い尾**。因子1本 0.7587 → 18本 0.7614 → 生134列 0.7636 |
>
> → **判定は「説明用の語彙」**（下の採否表）。**本番モデルには入れない**。
> 8ブロックを Phase 3・4 の語彙として採用する。
> ⚠ 並行分析は n=164,915 で帰無95%点が 1.05 まで落ち、**実質 Kaiser 基準に degenerate**
> した（弱い因子を止める力が無い）。事前登録した k の停止規則は機能しなかったので、
> **基準を緩めるのではなく計測器の限界としてそのまま記録した**。
> ⚠ 副産物: `f_blood` は **44%のレースで全馬同値**（ICC 0.96）＝馬の列ではなく
> レース条件の列になっている。血統を能力軸にするには先に `sire` を溜めるしかない。


| | |
|---|---|
| 仮説 | 馬側79列は、人が名前で付けた分類ではなく**データから出る少数の因子**で説明でき、その因子は元の列より良い表現になる |
| 使用データ | Phase 1b のCSV（馬側の列のみ。市場3列とレース共通10列は**入れない**＝§17⑤） |
| 方法 | 因子分析／PCA。**因子数は固有値と交差検証で決め、手で決めない**（§6 Step 2「型を人間が勝手に決めない」）。因子スコアを特徴量にして本番同型A/B |
| OOS期間 | W1〜W3 × 3シード |
| 評価指標 | AUC / Brier / LogLoss、および因子の解釈可能性（指示書 §3 A〜E の軸と対応がつくか） |
| **採用基準（本番モデルに足すか）** | AUCゲート 1〜4 |
| **中止基準** | **因子が2つ以下**（＝実質1軸で、分解する意味がない）→ この筋を閉じる |
| 🔑 AUC未達の扱い | **AUCが上がらない＝能力分解に価値がない、ではない**（2026-09-16 のレビューで確定）。AUC未達なら**因子を本番モデルに足さない**だけで、因子は Phase 3・4 が使う**語彙**として残す。Phase 3・4 はそれぞれ自分のゲートで判定する |
| ⚠ 歯止め | ただし無条件に先へ進めない。**Phase 3 の持ち越し基準**が通らなければ Phase 4 に進まない。**Phase 4 が AUCゲート未達ならこの筋を閉じる**（＝どこかで必ず止まる設計にする） |
| 既知 | 手で切った7軸では PCA 第1主成分 **39%**、軸間相関の中央値 0.27、**脚質は他軸とほぼ無相関**。1軸ではない。ただし軸内の重複が大きい（`f_speed_fig_avg × max` = +0.93） |
| 見込み | 🟡 軸が出ること自体は期待できる。**AUC が動く見込みは低い**（木は元の列を全部見ており、因子は言い換えになりやすい）。それでも Phase 3・4 の入口としては要る |

---

## 【旧 Phase 3・説明層として保留】コースDBを能力軸と同じ言葉で表現する

> ⚠ **2026-09-17 に番号を付け替えた。** 「Phase 3」の名前は
> **市場評価が結果に対して系統的に外れる条件を検証する**新しい枠組み（本ファイル後半）に移した。
> 以下の旧 Phase 3〜10 は**旧計画の番号のまま**残す。説明層としてのみ生きている。


> 🔴 **2026-09-17 のレビューで「AUCを上げる研究」としては原則ここで閉じた。**
> 根拠は4つ揃っている:
> ① XGBoost 自体が交互作用を表現できる
> ② 道悪 × 馬のタイプを明示的に手渡して **−0.0008**（2026-09-02）
> ③ コース物理量の未見会場への外挿は否定（2026-09-16）
> ④ 能力分解の追加予測寄与も非常に小さい（因子1本→18本で +0.0027）
>
> ✅ **残すのは「説明層」としての Phase 3・4。**
> `絶対能力 × 今日要求される条件 → 今日の能力発揮度` という**説明**を作れる可能性があり、
> 8つの能力ブロックはそのための語彙として価値がある。
> ⚠ ただし **買い目ロジックに直結させない**。まず説明層として検証する
> （出口は AUC ではなく、表示・説明・レース選択のどれかを**着手前に1つ選ぶ**）。


> ✅ **要求度は「実測から作る」側だけを残す**（2026-09-16 のレビューで確定）。
> 人が物理量から式を書く形は `f_course_stamina_demand`（gain **0.00%**）で
> 一度検証済みで、別の式にして掘り直すと恣意的な特徴量作りに入る。


| | |
|---|---|
| 仮説 | コースの物理量から「このコースが要求する能力プロファイル」を作れる。指示書 §6 Step 3 の形（馬 持続力0.8 × コース 持続力要求度0.7） |
| 使用データ | `data/course_starts.json`（127組・実レースの98.8%）＋ `race_history` |
| 方法 | 要求度は**勝手に定義しない**。実測から作る——そのコースで実際に好走した馬の Phase 2 因子スコアの平均を「要求度」とする（リーク防止: 対象レースより前のみ） |
| OOS期間 | 要求度の推定期間と評価期間を分ける。W1〜W3 |
| 評価指標 | ① 要求度が**年をまたいで持ち越すか**（前期→後期の相関） ② 未見コースへ外挿できるか |
| **採用基準** | ① 持ち越しの相関が **正・P<0.05**、かつ ② シャッフル対照群の相関が0付近 |
| **中止基準** | 持ち越さない（2026-09-15② の市場残差オフセットと同じ壊れ方）→ Phase 4 に進まない |
| 既知 | ✅ **ペースについては持ち越す**（前半600mのコース定数 ρ=+0.819, P=0.0005）。🔴 **市場残差については持ち越さない**（ρ=+0.158, P=0.20・上位セルは符号反転）。つまり**コースは走りを決めるが、市場はそれを完全に値付けしている** |
| 🔴 既知（2026-09-16 追加） | **未見コースへの外挿は成立しない**。leave-one-course-out の改善 -0.0153s は**1組（中山ダート1200）がほぼ全部**で、抜くと -0.0015s。**会場ごと未見にすると +0.0021s＝改善なし**。⚠ シャッフルの P=0.010 は通る——**Pは「正しい組に当てているか」しか見ておらず、1組が担いでいても通る**。この Phase の基準に**集中度の検査（上位1〜2組を抜いても残るか）を必ず入れる** |
| 見込み | 🟡 ①は通る可能性がある。ただし②（市場に残るか）は既に否定側。外挿も否定側 |

---

## 【旧 Phase 4・説明層として保留】能力 × コース適性の相互作用

| | |
|---|---|
| 仮説 | Phase 2 の能力軸 × Phase 3 の要求度を**明示的に**渡すと、木が自力で作るより良い。「総合能力1位だが今回は下げる」が数字で出る |
| 使用データ | Phase 1b のCSV + Phase 2 因子 + Phase 3 要求度 |
| 方法 | 交互作用は**5列以内**（69組・134列という規模に対し列を増やすと外挿が壊れる。2026-09-15③ で実測） |
| OOS期間 | W1〜W3 × 3シード |
| 評価指標 | AUC / Brier / LogLoss、**さらに「能力順位と今回期待順位が入れ替わった馬」だけに絞った成績** |
| **採用基準** | AUCゲート 1〜4 ＋ 市場比較ゲート 5〜6 |
| **中止基準** | ① Phase 2 が「因子2つ以下」で中止、または Phase 3 の持ち越しが通らない ② 3窓平均 +0.002 未満 ③ **条件を満たすレースだけに絞っても**改善しない（2026-09-02 の道悪と同じ形）。→ **②③ のいずれかなら、能力×コース適性という筋そのものを閉じる**（Phase 2 の「構造化としては残す」はここまで） |
| 既知 | 🔴 同型が3回とも -0.0003〜-0.0025。掛け算の形（`f_course_fit_score`）は**既に実装済み**で gain 0.55%、要求度の片側（`f_course_stamina_demand`）は**木が一度も使っていない** |
| 見込み | 🔴 低い。前回との差分は「軸をデータ駆動で作った点」だけ |

---

## Phase 5 — メンバーレベル（絶対能力と相対能力を分ける）

| | |
|---|---|
| 仮説 | §9 のとおり「絶対能力」と「今回のメンバー内での相対能力」は別の量で、分けた方が良い |
| 🔑 現状 | **相対化はもう入っている。しかも最大の塊**。`rl_*` / `cl_*` の **43列が gain の 38.0%** を占める |
| 🔴 既知の事故 | `f_member_level_*`（前走のメンバーレベル）は **2026-07-28 に未来参照リークで削除**。相手馬の**そのレース以降**の成績を使っていた（回収率261%の正体） |
| 使用データ | `history.db`。**予測日より前の相手成績のみ**（線引き: 予測日より後＝リーク／過去走から予測日まで＝合法） |
| OOS期間 | W1〜W3 |
| 評価指標 | AUC。加えて**能力差が大きいレース**に絞った的中率 |
| **採用基準** | AUCゲート 1〜4。さらに**リーク検査**——初出走馬の値が定数、同日内で不変、別実装との値の突合 |
| **中止基準** | ① リーク検査に1つでも落ちたら即中止（過去に一度これで撤回している） ② 3窓平均 +0.002 未満 |
| 見込み | 🟡 相対化が既に 38% を占めるため、追加の取り分は薄い。⚠ ただし「絶対 vs 相対」の**分離した表示**（§15）は Phase 8b で別に価値がある |

---

## Phase 6 — ペースモデルをレース記述として接続する（本体AUCは狙わない）

| | |
|---|---|
| 仮説 | ペースモデルは**レースの説明**として有用。本体精度の向上には使わない（§8 の天井を尊重） |
| 現状 | 前半600m OOS RMSE 0.8468s / P(先頭) AUC 0.7805。入力は**レース条件だけ**（距離・表面・クラス・頭数）で学習/推論の入力が構造的に同一 |
| 使用データ | `race_shape_model.pkl` |
| 評価指標 | ペース予測自体の RMSE のみ |
| **採用基準** | 表示・説明への接続のみ。**本体モデルの予測値を1ミリも変えないこと** |
| **中止基準** | 予測値・買い目が変わったら差し戻す |
| 既知 | 完璧なペースでも本体 +0.0007、展開系29列を全部抜いても -0.0001 |

---

## Phase 7 — Web情報の取得機構

| | |
|---|---|
| 🔴 制約 | この環境は egress proxy で**個別ページに到達できない**（`jra.go.jp` も `radionikkei.jp` も実測で EGRESS_BLOCKED）。使えるのは WebSearch の**要約**だけで、要約は複数の日付が混ざる |
| 🔴 制約 | **過去分が存在しない**ため学習データが作れない（North Star #3）。§11 の「後から結果を見て記事を取得しない」は自動的に守られる |
| 現状 | 層1（当日の馬場＝その日すでに終わったレースの結果から取得）は予想に接続済み。層2（webの記録）は `data/web_notes/` に**記録のみ**（`model_consumed: false` をスキーマが強制） |
| **Gate 1（§13）** | 収集可能性とカバレッジ。**週あたり何レース・何頭ぶん取れたか**を毎週記録する |
| **中止基準** | 3ヶ月続けて `official` のカバレッジが出走頭数の 10% 未満なら、この経路は打ち切って**パドック入力（ユーザー手入力）に一本化**する |

---

## Phase 8 — Web情報の構造化

| | |
|---|---|
| 現状のスキーマ | `data/web_note_schema.json` は **会場単位（馬場・天候・内柵）とレース単位（取消・乗替）**しか持たない |
| 🔴 ギャップ | §11 が求める**馬単位**の行（`horse_id, race_id, source, timestamp, field, value, confidence`）が**無い**。調教・陣営コメント・ローテは全部この単位 |
| やること | 馬単位の行を追加する。`field` は列挙（`training_time` / `training_note` / `comment` / `rotation` / `body_weight` / `paddock`）、`value` は文字列＋数値の両持ち |
| **§12 の分離** | `evidence_type` を必須にする: `stated`（陣営が明言した）/ `observed`（数字がある）/ `inferred`（Claudeの推測）。**`inferred` は特徴量候補にしない**。「今回は叩き」は原則 `inferred` |
| **採用基準** | 検証関数（`validate_web_note`）が `source` と `confidence` と `evidence_type` の無い行を**落とすこと**をテストで固定 |
| **中止基準** | なし（記録のスキーマなので、予想に触れない限り危険がない） |

### Phase 8b — 表示の分離（§15・独立して進めてよい）

| | |
|---|---|
| 目的 | 「強い馬」と「今回向いている馬」を別表示する。総合能力 / コース適性 / 今回条件適性 / メンバー比較 / Web情報 / 総合期待 |
| 現状 | `shap_explain.py` が `ability_margin` を15カテゴリに分解済み（画面に📊で表示） |
| やること | カテゴリを `HORSE_ABILITY_DESIGN.md` の仕分け（馬能力 / 今回の条件 / 掛け合わせ / 過去の市場）に**貼り替える**。表示のみ |
| **採用基準** | 「なぜこの馬を下げたか」が1行で読めること。**予測値を変えないこと** |
| **中止基準** | 予測値・買い目が変わったら差し戻す |
| 🔴 既知 | **SHAPの内訳から「AIを信じてよいか」は読めない**（-0.0038・2026-08-27）。説明であって選別ではない |

---

## Phase 9 — Web情報による最終補正を OOS で検証

| | |
|---|---|
| 仮説 | 調教・陣営・ローテには、市場が織り込んでいない情報が残っている |
| 使用データ | Phase 7〜8 で溜めた `official` / `observed` の行のみ |
| **Gate 3** | 過去データで同じ情報が取れるか（取れなければ学習データが作れない） |
| **Gate 4** | 情報ごとの**単独効果**。1つずつ測る |
| **Gate 5** | 市場との重複（市場残差に対する寄与） |
| **Gate 6** | OOS での再現性（前半→後半で符号が変わらない） |
| 必要N | **1,000頭以上**（パドック入力と同じ基準。週168頭で約3ヶ月） |
| **採用基準** | AUCゲート 1〜4 ＋ 市場比較ゲート 5〜6 |
| **中止基準** | ① N が基準に届かないうちは**特徴量化しない** ② 単独効果が全項目でヌル |
| 🔴 絶対禁止 | **後付けの補正レイヤーにしない。** 本プロジェクトで予想の出力を後から書き換える層は例外なく撤回されている（市場補正レイヤー / error_tags週次補正 / rank_matrix_filter / AI xx%バッジ / ROI予測150%）。入れるなら**上流（特徴量）に入れてA/Bを組む** |

---

## Phase 10 — 市場との比較と実オッズでの経済評価

| | |
|---|---|
| 対象 | Phase 2〜5・9 のうち、AUCゲートと市場比較ゲートを通ったものだけ |
| 使用データ | 実オッズ（netkeiba 53,916頭が artifact にある）＋ `race_dividends` の実配当 |
| OOS期間 | 2025-07-05〜2026-08-23（3,969レース・125開催日） |
| 評価指標 | 回収率、日ブロック bootstrap の95%CI、3本抜き、前後半 |
| **採用基準** | 経済ゲート 7〜10 |
| **中止基準** | CI下限が100%を超えない → **実装しない**（2026-07-27 の「基準未達でも入れる」で一度痛い目を見ている） |
| 見込み | 🔴 ここを通ったものはまだ無い。**通らないのが最も起こりうる結末**であり、先に書いておく |

---

## 依存関係と打ち切り

```
Phase 1 ✅──→ Phase 1b ──→ Phase 2 ──→ Phase 3 ──→ Phase 4 ──→ Phase 10
                               │     AUC未達でも      持ち越さ    ここが最終関門。
                               │     語彙として先へ    なければ止  未達ならこの筋を閉じる
                               │     （因子2つ以下なら止）  める
                               └──→ Phase 5（メンバー）

Phase 6（ペース＝記述）      … 独立。本体AUCは狙わない
Phase 7 → 8 → 9（Web情報）  … 時間が解決するまで記録のみ
Phase 8b（表示の分離）       … 独立。予測値を変えない
```

**止まる場所は3つある**（2026-09-16 のレビューで確定）。
① Phase 2 で因子が2つ以下 → この筋を閉じる（**2026-09-16: 8ブロック出たので①では止まらなかった。ただし追加寄与が1ブロックに集中しており、本番モデルへの投入はしない**）
② Phase 3 で要求度が年をまたいで持ち越さない → Phase 4 に進まない
③ Phase 4 が AUCゲート未達 → 能力×コース適性という筋を閉じる

⚠ **Phase 2 の AUC 未達だけでは止めない。** 能力分解は Phase 3・4 の語彙として
意味があるため（§19 の「総合能力1位だが今回は下げる」を言うには軸が要る）。
ただし ③ まで来て未達なら、そこで閉じる。

止まった場合に残るのは Phase 1b（掃除）・6（記述）・8b（表示）・7〜9（蓄積）で、
**いずれも回収率を上げない**。それがこの計画の最も起こりうる結末である。

---

## ✅ Phase 2 は正式に終了（2026-09-17）

**結論（この表現を使う。短縮しない）**
> 構造上は複数の独立した情報ブロックが存在するが、現データ・現目的変数・現モデルでは、
> 予測上の追加寄与の大部分が1ブロックに集中し、残りは薄い尾である。

- 8因子は年をまたいで **7/8 が ρ=0.82〜0.99 で持ち越す**。「能力は1軸しかない」とは**言わない**
- 予測寄与では 因子1本 0.7587 → 18本 0.7614 →（参考）生134列 0.7636 ＝ F1中心＋薄い尾
- 閉じるのは「**能力を細かく分解すれば本番モデルの精度を大きく上げられる**」という研究方向だけ
- 8ブロックは `絶対能力 × 今日要求される条件 → 今日の能力発揮度` の**説明用語彙**として残す。
  **買い目ロジックには直接接続しない**
- 並行分析の degeneration（n=164,915 で帰無95%点 1.05・k=26）は
  **「大標本ではこの方法がこの用途に向かない」という限界としてそのまま記録**。
  基準を後から緩めない。対照群B（k=2 を検出）は事前に通過済み。
  本数は Q3b（3着内を当てる力）で測り直したので**並行分析は再実行しない**
- `f_last1_rank`: A2−A1 = **+0.0091**（固有情報はある）／ B2−B1 = **+0.0002**（本番からは抜いてよい）。
  A3 は3窓とも正だが +0.002 未達 → **初出走と8着の衝突を理由にした追加探索はしない**

---

# Phase 3 — 市場評価が結果に対して系統的に外れる条件を検証する

**Phase 3 は「市場の考えを当てる研究」ではない。**
「市場評価が結果に対して**系統的に外れる条件**を検証する研究」として定義する。

## なぜ重点を移すのか

| 根拠 | 数字 |
|---|---:|
| 市場アンカーのみ → フル特徴量 | 0.8041 → 0.8050（**+0.0009**）|
| 能力を因子1本 → 18本 | 0.7587 → 0.7614（**+0.0027**）|
| 市場アンカーを外す／弱める | 3回試して3回とも悪化 |
| 道悪×型を明示的に交互作用として渡す | **−0.0008**（3/3窓マイナス）|
| コース物理特徴の未見会場への外挿（LOVO） | **+0.0021s ＝ 改善なし** |

→ **精密化の余地ではなく、情報源か問いの立て方を変えないと届かない。**

## 残っている方向は2つだけ

| | 方向 | 中身 |
|---|---|---|
| **①** | **市場がまだ価格に織り込んでいない新情報** | 追い切り評価 / 厩舎談話 / 当日馬体重 / その他、予測時点で新しく得られる情報 |
| **②** | **市場が見ている情報を誤って解釈しているケース** | 予測時点で利用可能だった情報 X と市場評価の関係、および X と実績との残差 |

---

## 🔴 ② を始める前に必ず満たす3条件（これが無いと F-4 の再演になる）

②は構造的に「条件を探す」形なので、**何も対策しないと必ず偽陽性が出る。**
このプロジェクトは同じ形で5回負けている。

| 時期 | 見たセル数 | 結果 |
|---|---:|---|
| 2026-08-15② | 25 | 100%超は1マスだけ（N=285・多重比較の期待値内）|
| 2026-08-24 F-3 | 246 | 100%超10セル／偶然の期待値12セル ＝ **期待以下** |
| 2026-08-24 F-4 | 41 | Δ の順位が前後半で **r = −0.001**（並べ替え検定 P=51%）|
| 2026-08-28 MF | 90 | 選抜期で良かった30組のうち検証期も良いのは **3組 = 10%**（偶然なら50%）|
| 2026-08-31 | 25 | 緑5マス／帰無の期待 3.7マス（5〜95%点 1〜6）|

🔑 **F-4 が示したのは「偏りが無い」ではない。**
偏り（Δ≈+10pt）は 50,000頭規模で一貫して存在した。持ち越さなかったのは
**「どの条件で偏りが強いか」の順位のほう**（1400-1600m は +26.1 → −3.4、小倉は −28.0 → +26.9 と反転）。

### 条件1: **X は「予測時点で計算できる連続量・順序量」として定義する**

条件（部分集合）で始めない。

| ✕ 書かない | ○ 書く |
|---|---|
| 「前走を誤解している**条件**」 | 「前走着順と、前走の走破内容から作った指標との**乖離量 X**」 |

変数形にすると ①下のラダーがそのまま使える ②セル数を数えなくてよい
③傾き1本で検定できるので検出力が上がる、が同時に解決する。

⚠ どうしてもセル形式で始めたい場合は、**先にセル数を宣言して帰無シミュレーションの期待個数を出す**
（2026-08-31 と同じ手順）。これをカードに書けないならセル形式で着手しない。

### 条件2: **X は「レース内で変化する量」に限る**

**1レースで3着内は必ず3頭なので、レース内の市場残差の和は構造的に ≈0 に固定される**
（2026-09-15 実測: レース内残差の和の std 16.4pt／独立なら 155pt・|和|<5pt が 87.5%）。

> **レース内で一定の属性でまとめると、市場が何をしていようが 0 に潰れる。**
> しかもラベルをシャッフルする対照群A は ρ≈0 で「✅計測器は正常」と通る（偽セルも同じ理由で0）。

- 会場・距離・馬場・クラスのような**レース定数を X にしない**
- 使うならレース内中心化した後に何が残るかを先に示す
  （`x` と `x_vs_field` の17対が本番で −0.0001 だったのと同じ構造。原理的に効きにくい）
- **新しい測定器には必ず対照群B（既知の効果を注入して検出できるか）を、本データを見る前に通す**（North Star #11）

### 条件3: **「持ち越すか」を頑健性検査ではなく一次基準に置く**

```
期間を A（選抜）/ B（検証）に分割
  → A だけで X の係数（または条件の順位）を決める
  → B で「A で決めた定義・向き・モデル仕様を一切変えずに」評価する
※ A に B のデータを1行も使わない（2026-08-24 F-4 で踏みかけた選択汚染）
```

**前後半で符号が反転したら「弱い効果」ではなく「効果が無い」と読む。**
ここを曖昧にすると全部が生き残る。

### 🔑 「持ち越し」の測り方は X の形で決まる（2026-09-17 に明文化）

「順位相関」という言葉を曖昧に使わない。**X が連続量1本のときに係数の順位相関を
取っても意味がない**（並べる対象が1つしかない）。

| X の形 | 持ち越しの測り方 |
|---|---|
| **連続量が1本**（P3-01 はこれ） | **βA と βB の符号一致 ＋ B での効果量 ＋ βB の信頼区間**。順位相関は使わない |
| 事前固定のビン・セルが複数 | ビン間の順位相関 ＋ 並べ替え検定（2026-09-15 の `zansa/z2.py` がこの形）|

⚠ 連続量の β を推定するとき、**素の OLS の標準誤差を使わない**。
レース内の残差は「3着内は必ず3頭」の制約で強く負に相関しており、
独立を仮定した SE は必ず狭すぎる（実測: レース内残差の和の std 16.4pt / 独立なら 155pt）。
**レースを1ブロックとするブロックブートストラップ**で区間を出す。

### 🔑 「情報がある」と「市場に優位がある」を分ける（2026-09-17 に明文化）

| 測るもの | 言えること |
|---|---|
| **X → 結果** が強い | 「X には**予測情報**がある」だけ |
| **X → 市場残差** が安定 | 「市場が X を十分に評価していない**可能性**」 |
| **両方**が安定 | ここで初めて馬券上の市場ミスとして検討する価値が出る |
| X → 市場残差だけ強く、X → 結果が無い | **慎重に扱う**。残差の作り方の副産物である可能性を先に疑う |

仮説カードには両方を書く（前者が Secondary の S1、後者が Primary）。

---

## 🔑 測定器を主張に合わせる（2026-09-17 に一般則として追加）

`f_last1_rank` の件（線形残差 AUC 0.5047 を根拠に「固有情報が無い」と断定 → OOSアーム比較で +0.0091・誤りだった）
を受けて、**主張ごとに対応する測定方法を仮説登録時に決める**ことを必須にする。

| 主張 | 使う測定器 |
|---|---|
| X には独立情報が無い | **X なし vs X あり**（アーム比較）|
| X の非線形性が重要 | 線形X vs 非線形X |
| X は本番モデルから削除できる | Production vs Production − X |
| X は市場を超える | Market vs Market + X |

- 🔑 **「本番から抜いてよい」と「固有情報が無い」は別の主張。** 片方しか測っていないのに他方を書かない
- 🔑 重要な**否定**結論は、可能なら**異なる測定原理を2つ以上**で示す。
  ⚠ ただし「2つ以上」は**データ経路が異なる2つ以上**を意味する。
  PL レーティングのときは θ を3通り検査して**全部通った**（初出走馬が厳密に0／同日内で不変／着順との相関）。
  同じ計算列に統計を3つ当てても、経路が同じなら同じ嘘を3回見るだけ
- ⚠ **結果を見てから測定器を足さない。** 仮説登録時に決める

---

## ラダー: 「X単独の情報価値」と「市場に対する追加価値」を分離する

```
X単独          … X にそもそも情報があるか
Market         … 基準
Market + X     … 市場に対する追加情報があるか      ← ②の主戦場
Market + AI    … 現行の到達点
Market + AI + X … 現在のAIを超える追加情報か        ← 最終確認
```

- `X単独` が強くても `Market + X ≒ Market` なら、
  「X に情報が無い」のではなく**「市場が既にその情報を持っている」**
- `Market + X > Market` で初めて市場に対する追加情報の候補

### ⚠ ラダーを組むときの実装上の注意（このシステム固有）

**残差学習では市場は `base_margin` からしか入らない。** したがって
「Market + X」を**134列側に足すのか `base_margin` 側に入れるのか**で別物になる。
**カードには2択のどちらかを明記する**（抽象的な「Market + X」と書かない）。

### ⚠ 解像度の問題（`Market + AI + X` を主要基準にしない）

```
Market only（人気順アンカー）      AUC 0.8041
Market + AI（129特徴量フル）       AUC 0.8050    ← AI全体で +0.0009
シード std                                 0.0007
```

**`Market+AI+X > Market+AI` を +0.002 で要求すると、X 単独に「134特徴量ぜんぶの2倍以上」を求めることになる。**
よって:

1. **主戦場は `X単独` と `Market + X`**。`Market+AI+X` は最終確認であって、
   ここで落ちたことを「X に価値が無い」と読まない（**現AIとの重複**であって、市場との重複ではない）
2. **AUC を ② の主指標にしない**（②は較正の主張）。主指標は市場残差＋その持ち越し
3. **「正だが解像度未満（+0.0005〜+0.0015）」のときの行き先をカードに事前に書く**。
   窓・シードを増やして SE を下げるのか、そこで閉じるのか

---

## 残差 → 回収率の換算レート（**探す前に**宣言しておく）

North Star #9 の実務版。**+2pt の残差が出たときに、その場で嬉しさを判断させないための仕掛け。**

| 実測済みの最大級の残差 | 市場残差 | 複勝回収 |
|---|---:|---:|
| 全馬（ベースライン） | −0.37pt | 69.9% |
| 回顧の上限（前走10着以下 × 上がり上位20%） | +1.18pt | 74.0% |
| PL実力上位（PL1-3・3走以上） | **+1.67pt** | 79.6% |

粗い外挿で **+1.7pt ≈ +10pt ROI**。損益分岐（70% → 100%）に必要なのは概算 **+5pt 前後の残差**で、
これは過去に測った最大値の**3倍**。線形とは限らないが桁は合っている。

→ **カードに「この仮説が成功したとき、残差は何ptを期待するか」を事前に書く。**

---

## 「失敗原因は今回消えるか」は反実仮想。因果語を測定可能な形に翻訳する

「市場の内部思考を断定しない」という規律は**「失敗原因」側にも同じく必要**。
「前走の敗因は不利だった。今回は消える」は因果の主張で、観測データからは識別できない。

| ✕ 書きたいこと | ○ カードに書く形 |
|---|---|
| 前走の敗因は不利で、今回は消える | 予測時点で計算できる指標 X（前走の走破内容と着順の乖離量）が、**今回の市場残差の符号を予測するか** |

測れるのは「機械的に定義した X が市場残差を予測するか」だけ。**因果は語らない。**

---

## 禁止情報は散文ではなく**実行可能な assert** にする

カードに「予測時点／利用可能な情報／禁止情報」を書くのは必要だが、**それだけでは機能しない。**
このプロジェクトのリークは**1件も文書チェックで捕まっていない**。

| リーク | 文書では | 実際に捕まえたもの |
|---|---|---|
| `member_level`（相手馬の未来成績）| 記述なし | 回収率261%という異常値 |
| `first_3f`（そのレース自身）| 記述なし | 学習/推論の入力突合 |
| PL θ のタイブレーク | θ の検査は**全部通過** | 本番パイプラインで別実装して値を突合 |
| `agari3f`（当該レース）| 記述なし | 学習側と推論側の馬辞書のキー突合 |

→ **`assert_no_future_access(horse, race, as_of)` のような実行可能な形にする。**
通らなければ**結果を印字せず中止**（コースDB §18 の感度検査と同じ順序）。

---

## 📋 仮説カード（Phase 3 のすべてのテーマで着手前に登録する）

```
仮説ID              : P3-xx
一行の仮説           :
方向                 : ① 新情報 / ② 市場の誤読

■ 定義
予測時点             : （例）当日 08:00 の予想生成時点
X の定義             : 予測時点で計算できる連続量・順序量として書く
X はレース内で変化するか : yes / no（no なら条件2に従い着手しない or 中心化後に何が残るか先に示す）
利用可能な情報        :
禁止情報             : ＋ assert 関数名（実行可能であること）

■ アーム（残差学習のどこに X を入れるかを明記）
X単独 / Market / Market + X / Market + AI / Market + AI + X
X の投入先           : 134列側 / base_margin 側

■ 主指標と副指標
主指標               : （②は原則 市場残差。AUC を主指標にしない）
副指標               :
期待する残差          : ＋pt（換算レート表を見て書く）
「正だが解像度未満」のとき : 窓/シードを増やす / そこで閉じる

■ 検証
OOS 期間             :
選抜期 A / 検証期 B   : （持ち越しは一次基準）
乖離方向別            : AI強気 / 一致 / AI弱気
対照群A（並べ替え分布・レース内シャッフル200回）: 合格 |mean(β_shuffle)| < 2*std/sqrt(200)
対照群B（注入の復元）                      : 合格 「注入後 − 注入前 = δ」（許容 1e-3）← 本データを見る前に通す
セル形式の場合のセル数と帰無の期待個数 :
焼き直し検査（既に閉じたカードの別表現でないか）: 相関の上限を数値で置き、超えたらカードを無効にする

■ 判定
成功基準             :
中止基準             :
測定の予算            : （何回測ったら止めるか）
```

⚠ **中止基準と予算を空欄にしない。** 書かないと仮説が無限に精緻化される。

### 🔑 対照群はこの2つの形で書く（全 Phase 3 に適用・2026-09-17 確定）

| | 合格式 | 書いてはいけない形 |
|---|---|---|
| **対照群A** 並べ替え分布（レース内シャッフル200回）| 帰無が0中心 ＝ `\|mean(β_shuffle)\| < 2·std/√200`。観測βは**並べ替え P** で報告 | `\|β_shuffle\| < \|β\|/3`（**1回の引きの大小比較**。P3-01 では閾値 0.37 が推定量の SE 0.65 より小さく、正常でも半分以上落ちた）|
| **対照群B** 注入の復元 | **`注入後 − 注入前 = δ`**（許容 1e-3）| `注入後 ≈ δ`（「その変数の素の効果は0」という**測っていない仮定**が入る。P3-01 の別経路 Z は素の効果 −1.03pt）|

🔑 **統計量に閾値を置いたら、その閾値がその統計量自身の SE より大きいかを先に確かめる。
SE より小さい閾値は「厳しい基準」ではなく「何も判定できない基準」。**
⚠ 対照群B の許容差は**代数恒等式の検算**なので 1e-3 でよい（統計量の閾値と混同しない）。
詳細と実例は `mispricing/CRITERIA.md` §3.1 ／ North Star #11。

### 登録済みのカード

| ID | 仮説 | 場所 | 状態 |
|---|---|---|---|
| **P3-01** | 前走の「着順」と「内容」の乖離量 X が大きいほど市場残差が正に動く | `mispricing/P3-01.md` → `RESULTS_P3-01.md` | 🔴 **不採用で閉じた**（2026-09-17・**2026-09-18 にユーザーが終了を確定**）。符号は2期間とも予測どおり正（βA +1.277 / βB +1.107）だが**効果量 +0.78pt**で信号ライン(+2pt)にも届かない。CIは両期間とも0を含む。Secondary の S1（X単独AUC）は **0.4297 / 0.4269** で 0.5 を下回り、X は「前走着順が悪い」を測っていただけ。**定義を変えて再挑戦しない** |

| ~~**P3-02**~~ | 実績の薄さによる縮小ギャップ `X = naive − shrunk` が大きいほど市場が過大評価している | — | 🔴 **算術上の天井により未測定で閉じた**（2026-09-18）。使う軸の主効果が `f_career_runs` +1.08 / `f_recent_fukusho` +1.33pt しかなく、そこから +6pt の交互作用が出ると考える根拠が無い。加えて X は「見栄えが良いのに薄い」＝**過大評価側**を指すので、**買える側の幅が構造的に小さい**（＝切る馬の話）。⚠ 「効かないと分かった」ではなく「**測定予算を使う価値が無いと分かった**」 |
| **Gate 0** | 現在の特徴量で、**能力と独立した今回条件への適合度（適性）**を作れているか | `betting/CRITERIA.md` | 🟡 **登録済み・未測定**。Betting Policy 研究（能力×適性→馬券変換）の入口。落ちたらその研究は終了 |

### 🔴 Phase 3 ② は閉じた（2026-09-18）

②-B の枠は**最大2本**と決めてあり、**2本とも使い切った**
（1本目 P3-01 = 測って不採用 / 2本目 P3-02 = 算術上の天井により未測定で終了）。
②-A（主効果131軸）は 2026-09-10 に**正の残差の上限 +3.23pt** で閉じている。

> **既存データの範囲では、市場評価の歪み（direction ②）は控除率を超える大きさでは
> 見つからなかった。②で残るのは①（予測時点でまだ市場に入っていない情報）だけである。**

⚠ 「市場に歪みは無い」ではない。「**既存データで測れる範囲には無い**」である。
⚠ ②を再開したくなったら、まず `mispricing/CRITERIA.md` §5 の確定文を読むこと。
  新しい情報源（①）が入るまで、②の中で X を作り直しても同じ壁に当たる。

### 2026-09-18 以降の2本立て

| | テーマ | 出口 | 状態 |
|---|---|---|---|
| **①** | 予測時点でまだ市場に入っていない情報 | 市場残差 | インフラ整備から（公開時刻 / 予測時点で利用可能だったか / 出典 の3列） |
| **Betting Policy** | AI が**既に持っている**情報（能力・適性）を馬券の形に変換できるか | **単勝・複勝の実回収率** | `betting/CRITERIA.md` の **Gate 0 のみ登録済み** |

🔴 **Betting Policy は②の続きではない。** ②は「市場が間違える領域」を探す研究、
こちらは「手持ちの情報の変換効率」を測る研究。
**Step 2（能力×適性 → 市場残差）は最後まで記述のみで、選択には一切使わない**
——使うと②-B の交互作用探索と実質的に同じものになるため。

---

## ①（新情報）の着手条件

**「有料データだから既に市場に織り込まれている」とは事前に判定しない。**
有料か無料かだけでは市場への影響度は分からない。取得できたら小規模な事前スクリーニングで
`X単独 / Market / Market + X` を見る（本格取得の前に「市場との重複」を安く測る）。

### ただし、いま揃っていない前提が2つある

| 前提 | 現状 |
|---|---|
| **その情報を継続的に取得できるか** | JRA公式に機械可読な調教タイムは**存在しない**（2026-08-16 に実HTMLで4回検証）／netkeiba の追い切りは**有料**（2026-08-24）／この環境は egress proxy で**個別ページに到達できない**（`jra.go.jp`・`radionikkei.jp` とも EGRESS_BLOCKED を実測）|
| **公開時刻とオッズ変化の時間関係を見られるか** | ⚠ **現状の観測密度では測れない。** 旧データは手動ボタン頼みで 405レース中3回以上取れたのは**55レースだけ**。自動収集（2026-08-28 導入・土日 09:20/15:00 JST）は稼働中だが N=2,000 規模まで**約7ヶ月** |

🔑 **後者は「ゲート」ではなく「①の前提条件（依存）」として扱う。**
前提が揃うまで①は着手不可。後から「時間管理は省略」と流れるのを防ぐため明記する。

### ⚠ ただし「7ヶ月なにもしない」ではない（2026-09-17 に追記）

**「①は検証できない」と「①の準備を止める」は別物。** ②を進めている間、①については
**研究ではなく「研究可能な状態を作る準備」**を並行させる。具体的には、後から時間関係を
検証できる形で次を保存しておく:

| 保存するもの | 現状 |
|---|---|
| オッズ取得時刻（JST・秒まで） | ✅ `odds_snapshots.captured_at` |
| 発走までの残り時間 | ✅ `odds_snapshots.minutes_to_post`（2026-08-28 追加）|
| そのスナップショットの人気順・オッズ | ✅ `odds_snapshots` |
| 取得の完全性（何頭取れたか / 取消か） | ✅ `n_captured` / `n_expected` / `is_scratched` |
| **情報の公開時刻** | ❌ **未保存**。①の情報源が決まってから設計する |
| **その情報が予測時点で利用可能だったか** | ❌ **未保存**。上と同時に設計する |
| 取得元（どのページ・どの経路か） | 🟡 `data/web_notes/` は `source` を持つが `odds_snapshots` は持たない |

### 🔎 候補情報源の実地確認（2026-09-22・1件のみ）

ネットワークポリシーが Custom になり、**個別ページに実際に到達できるようになった**
（WebFetch も `curl` も通る。前提が1つ解消された ⇒ `data/web_note_schema.json` の
`why_not_consumed` にある「個別ページは egress proxy に阻まれる」は**もう当たらない**）。

そこで許可リストのうち1件（`koukakukeiba.jugem.jp`）だけ、3列の要件を満たすかを見た:

| 要件 | 結果 |
|---|---|
| 到達できるか | ✅ WebFetch / curl とも（12,403 B） |
| **公開時刻が取れるか** | 🟡 **取れるが脆い**。`<time>` も `datetime=` も `article:published_time` も**無く**、本文に `2026.09.21 Monday` と `20:43` がプレーンテキストであるだけ |
| **予測時点より前に公開されているか** | ✅ 9/22 のレース予想が **9/21 20:43** に公開。土日朝の予想生成より前 ⇒ リークにならない |
| 中身 | 予想印（「14番 24% 58%」形式）。市場オッズへの言及なし |

🔑 **設計への含意**: `published_at` は「取れることもあるが構造化されていない」ので、
**取得できた場合のみ記録し、取れなければ null（推測しない）**。既存スキーマの
「分からないものは書かない。空欄のままにする」と同じ扱いにする。
⚠ 正規表現で本文から拾う形はテンプレート変更で静かに壊れる
（2026-08-03 の結果ページ列崩れと同型）。**壊れたら null になる**設計にすること。

⚠ **これで情報源が確定したことにはならない**（見たのは 11ドメイン中1つ）。
🔴 **2026-09-23 に残り10件を見た結果、この節の「公開時刻は取れるが脆い」は
1件からの一般化で誤りだった**（RSS/Atom を持つ情報源が2件あり、そちらは構造化されている）。
下の「許可ドメイン11件の実地確認」を正とすること。
⚠ そして中身が**予想印**である以上、CLAUDE.md 2026-09-22 の整理がそのまま効く:
**集約された個人予想の完成形は既に市場オッズ**であり、測るべきは印の平均ではなく
`集約印のレース内順位 − 市場人気順位`。**事前見積りは +0 が最も可能性が高い**。

**準備として足りないのは下の3行だけ**で、いずれも①の情報源が確定してからでないと
列の形が決められない。**情報源が決まった時点で最初にやるのがこの3列の設計**、と
順序をここに固定しておく（「集め始めてから時刻を付け忘れていた」を防ぐため）。

⚠ これは**研究ではない**ので、①の前提条件が揃ったことにはならない。
N=2,000 規模の観測密度が要るという事実は変わらない。

✅ 例外的に手元にあるのは**パドック採点**（2026-09-02 に入力系を新設・蓄積のみ・予想には未接続）。
ユーザーしか持っていない唯一の情報で、市場にもDBにも無い。N=2,000 に約3ヶ月。

### 🔎 許可ドメイン11件の実地確認（2026-09-23・残り10件を全部見た）

2026-09-22 は `koukakukeiba.jugem.jp` の1件しか見ていなかった。残り10ドメインを
`curl` と `WebFetch` で実際に叩き、**robots.txt → 利用規約 → 到達性 → 公開時刻の取れ方 →
中身**の順に確認した。**1ドメインあたり数リクエストに抑え、`Crawl-delay` を持つ
サイトでは間隔を空けた。**

| ドメイン | 到達 | robots.txt | 公開時刻 | 中身 | 判定 |
|---|---|---|---|---|---|
| `*.jra.go.jp` | ✅ 200 | `Disallow:` **空＝全許可**（2026-08-16 に実読・再確認） | — | 出馬表・結果・オッズ | ✅ **本番の取得元。既存** |
| `db.netkeiba.com` | ✅ 200 | **404**（許可も禁止も明示なし） | — | 実オッズ・結果 | 🟡 規約で私的利用限定。**①ではない** |
| `kayochinkeiba.com` | ✅ 200 | WP標準・実質全許可 | ✅ **RSS `pubDate`（RFC822）** | 印＋**連続量の能力指数 41〜91**。市場オッズ言及なし | ✅ **①候補** |
| `*.bloodline-trackbias.work` | ✅ 200 | はてな標準・実質全許可 | ✅ **Atom `<published>`（ISO8601）** | **レース単位のトラックバイアス予想**（枠／直線の伸び／前後有利・散文）。市場オッズ言及なし | ✅ **①候補** |
| `koukakukeiba.jugem.jp` | ✅ 200 | （2026-09-22 は未取得） | 🟡 本文のプレーンテキストのみ | 🔴 **2026-09-24 訂正: 「予想印」は誤り。9カテゴリ。調教タイムが出る一方、朝イチオッズ分析はオッズを読む。EUC-JP** | 🟡 ①候補（下記の訂正を読むこと） |
| `*.jbis.or.jp` | ✅ 200（**curl のみ。`WebFetch` は 403**） | `Crawl-delay: **600**`／`/premium/` `/csv/` 等 Disallow／**`/horse/` は許可** | — | **父・母の父・調教師の所属(栗東/美浦)・生産牧場・セリ価格** | 🟡 **①ではない。既存の穴を埋める経路**（下記） |
| `p.keibabook.co.jp` | ✅ 200 | 🔴 **`User-agent: *` / `Disallow: /`（全面禁止）** | — | — | ❌ **取得しない** |
| `*.nikkansports.com` | ✅ 200 | 🔴 **`anthropic-ai` / `Claude-Web` / `GPTBot` / `CCBot` を名指しで `Disallow: /`** | — | — | ❌ **取得しない** |
| `*.sponichi.co.jp` | ✅ 200 | 🔴 **`ClaudeBot` / `Claude-User` / `Claude-SearchBot` / `Claude-Web` / `anthropic-ai` / `Scrapy` を名指しで `Disallow: /`** | — | — | ❌ **取得しない** |
| `*.keibalab.jp` | ❌ **8回すべて失敗** | 取得不能 | — | — | ❌ **到達不能** |
| `*.tospo-keiba.jp` | ❌ www→apex の 301 で止まる | 取得不能 | — | — | ❌ **許可リストの書き方の問題**（下記） |

#### 🔴 測る前に、11件中6件が落ちた

**落ちた理由が「効かない」ではない。** 規約・robots・到達性という**手前の条件**で落ちている。

- **robots が明示的に禁じている 3件**（競馬ブック／日刊スポーツ／スポニチ）。
  特に後者2件は **`anthropic-ai` / `Claude-Web` / `ClaudeBot` を名指し**しており、
  「禁止と書いていないから良い」という読み方の余地が無い。**取得しない。**
- **到達できない 2件**。
  - `www.keibalab.jp` は **8回すべて失敗**（`curl` が Connection reset、`WebFetch` が 503）。
    同じ時間帯に `www.sponichi.co.jp` が一度だけ同じ失敗をして**再試行で回復**したので、
    relay の一時障害と区別するために回数を重ねた。keibalab だけ回復しない。
    🔑 ただし **原因が site 側（データセンターIP 拒否等）か relay 側かは切り分けられていない。**
    「該当なし」ではなく「**この環境からは到達できない**」とだけ書く（2026-08-16 の教訓）。
  - `www.tospo-keiba.jp` は**全パスが `https://tospo-keiba.jp:443/` へ 301** し、
    その apex を **egress proxy が 403 で止める**。
    🔑 許可リストの `*.tospo-keiba.jp` は **apex の `tospo-keiba.jp` を含まない**。
    `keibalab.jp` の apex も同じ 403。**直したければネットワークポリシーに apex を
    別エントリで足す**（`tospo-keiba.jp` / `keibalab.jp`）。コード側の問題ではない。

#### ✅ 設計への含意①：公開時刻は「脆い」で一般化してはいけなかった

🔴 **2026-09-24 追記: kayochin は OG の `article:published_time` を使う。**
同じ記事に `+00:00`（OG・RSS pubDate と一致・正）と `+09:00`（JSON-LD・誤り）が
併記されており、JSON-LD を信じると**9時間ずれて「予測時点で利用可能だったか」が逆になる**。
取得経路の優先順は `feed > OG meta > ページの <time datetime> > 本文の正規表現 > null`。

2026-09-22 に jugem 1件だけを見て「`published_at` は取れるが脆い（本文の正規表現頼み）」
と書いたが、**1件からの一般化だった**。実際に10件見ると:

| 取り方 | 該当 |
|---|---|
| **RSS `pubDate` / Atom `<published>`** ＝ 構造化・堅牢 | kayochinkeiba / bloodline-trackbias |
| 本文のプレーンテキストのみ ＝ 脆い | jugem |

🔑 **正しい設計指針は「壊れたら null」ではなく、その手前に「RSS/Atom を持つ情報源を選ぶ」**。
本文の正規表現は**最後の手段**。`published_at` を取る経路は
`feed > ページの <time datetime> > 本文の正規表現 > null` の順に落とす。
⚠ 「壊れたら null になる設計にする」こと自体は引き続き正しい（2026-08-03 の列崩れと同型）。

#### 🔴 設計への含意②：公開時刻が本番の実行時刻とぶつかる

`bloodline-trackbias` の予想は **前日 19:00 / 20:00 固定**で公開される。
本番の週末ワークフローは **19:06 JST 開始**（2026-09-19 の実ログ）。

```
金 19:00  トラックバイアス予想（土曜ぶん）公開
金 19:06  friday-predict が土曜予想を生成      ← ぎりぎり間に合う（6分）
土 20:00  トラックバイアス予想（日曜ぶん）公開
土 19:06  weekend(saturday) が日曜予想を生成   ← 🔴 54分早い。まだ記事が無い
```

🔑 **「到達できる」と「予測時点で手に入る」は別物**。①の定義そのもの
（＝予測時点でまだ市場に入っていない情報）に照らすと、**記事が予測時点より後なら
そもそも使えない**。使うなら ①予想生成を遅らせる ②直前オッズを押した時点で取りに行く
のどちらかが要る。**3列のうち「予測時点で利用可能だったか」は、この情報源では
飾りではなく本当に効く列になる。**

#### 🔴 いちばん大きい発見：「集約」が成立しない

ユーザーの要望は「**個人の見解も含めたあらゆる情報を分析して予想する。1人の見解に
意味はない**」だった。しかし許可リスト11件を全部見た結果、①の情報源として
使えるのは **`kayochinkeiba` / `bloodline-trackbias` / `jugem` の3件だけ**で、
うち予想印を出すのは2件（kayochin・jugem）。

> **2人の印を平均しても「集約」ではない。**

CLAUDE.md 2026-09-22 の整理（集約された個人予想の完成形は既に市場オッズ／測るのは
`集約印のレース内順位 − 市場人気順位`／事前見積りは +0）は、**そもそも「集約」に
必要な人数が集まらない**という、もう一段手前の理由で効いてくる。
⚠ これは「①を閉じる」ではない。**印を集める筋が細い**というだけで、
`bloodline-trackbias` の**トラックバイアス（レース単位の馬場読み）は印ではなく別の軸**。

#### 中身の内訳（①候補3件）

| | kayochinkeiba | bloodline-trackbias | jugem |
|---|---|---|---|
| 形 | 馬ごとの**印＋能力指数(41〜91)**。**全12レース・全馬に付く** | **レース単位**（会場×馬場）の枠／直線／前後の有利不利・散文 | **9カテゴリ**（軸馬＋3着内率／調教注目馬／加速ラップ馬／重不良適性ランク／朝イチオッズ分析 ほか） |
| JRA のカバー | その週の開催場すべて（例 9/19-22 は中山・阪神）。**主戦場は地方競馬** | 開催場すべて（3場開催時も3場） | 1日ぶん |
| 公開 | 前日 18:46〜19:39 JST | 前日 **19:00 / 20:00 固定** | 前日 19:45〜20:43 ＋ **当日朝 07:42〜07:46** |
| 市場オッズへの言及 | なし | なし | 🔴 **ある**（朝イチオッズ分析）。そのカテゴリは定義上市場の言い換え |
| 過去分 | ✅ **sitemap に 2016-12〜（119ヶ月）。2023-01〜2026-09 で JRA記事 811本**。2023年版と2026年版が同一構造 | 未確認 | 未確認 |
| 事前見積り | 🟡 印は +0（上記）。**能力指数は連続量なので印より情報量はある** | 🔴 **低い**。馬場バイアスは **3回否定済み**（2026-08-06 当日バイアス -0.0005 ／ 2026-08-26 `f_bias` を繋いで -0.0005 ／ 2026-08-27 馬場状態の反映を3段階で否定）。枠の不利を拾う向きも 2026-08-24 に**仮説と逆**と実測 | 🟡 印は +0 |

⚠ **3件とも「測れば分かる」段階ではない。** 事前見積りが +0〜低で揃っているので、
着手するなら**先に測定器（3列）を作り、N が溜まってから**という順序は変わらない。

#### 🟡 ①ではないが見つかったもの：JBIS が血統と調教師所属を持っている

`www.jbis.or.jp/horse/{10桁ID}/` の**無料ページ1枚**から実際に取れた（アドマイヤズームで確認）:

```
父   モーリス
母   ダイワズーム ─ その父 ハーツクライ     ← 🔑 母の父（dam_sire）が取れる
調教師 友道 康夫 (栗 東)                      ← 🔑 所属（栗東/美浦）が付いている
ほか 生年月日・性別・毛色・産地・馬主・生産牧場・セリ価格・年度別成績
```

これは CLAUDE.md が長く抱えている2つの穴に直接当たる:

| 穴 | 現状 | JBIS なら |
|---|---|---|
| `sire` 充足 **0.8%**（163,402行中1,362行）。「JRAは血統ページのCNAMEを**出馬表からしか**出さないので**引退馬には永久に到達できない**」（2026-09-02） | 実測ペース約161頭/週 ⇒ 2026年出走馬9,466頭に**約1年** | **ID ベースの静的URLなので引退馬も引ける** |
| `trainer_affiliation`（栗東/美浦）**0%**（2026-07-21⑥ の導入以来）。JRA の結果ページに所属表記が無いと 2026-08-03③ に確定 | 永久に埋まらない | **同じ1ページに入っている** |

🔴 **ただし2つ、先に片付けないと着手できない。**

1. **`Crawl-delay: 600`**（10分に1リクエスト）＝ **144頭/日**。9,466頭で**約66日**。
   North Star #4 の budget を必ず設ける。JRA 経路（budget 500 で約4ヶ月）より速いが、
   1日でも早く欲しい類の話ではない。
2. 🔴 **利用規約 第8条がかなり厳しい**（`https://www.jbis.or.jp/terms/` を実読）:
   - 1項「**個人的な利用**または法人（軽種馬生産・育成牧場に限る）の内部利用に限る」
   - 2項「**加工・編集することなくあるがままに購読し、利用するものとします**」
   - 5項「**第三者が自由に接することができる場所に置き、自由に取得させることを禁じます**」

   🔑 **2項は「特徴量化＝加工」を正面から禁じていると読める。** netkeiba（私的利用に
   限定しているが加工禁止条項は無い）**より厳しい**。
   🔑 5項があるので、**公開リポジトリには置けない**（netkeiba と同じく `data/private/`）。

   → **Claude が「禁止と書いていないから良い」とも「私的利用だから良い」とも判断しない。**
   netkeiba を 2026-08-16 に「規約上グレー・ユーザー判断待ち」とした時と同じ扱いにする。
   **着手するならユーザーの明示的な判断が要る。**

⚠ そして**血統を入れれば当たる、は一度も実証されていない**。Phase 2 の実測では
`f_blood` は ICC **0.9604**・全馬同値のレースが **44.0%** で、馬の列ではなく
**レース条件の関数に退化**していた（2026-09-16）。JBIS が解消するのは
「データが無い」であって「効く」ではない。


## ②（市場の誤読）の着手条件

- 「前走の結果は本当に能力を表しているか」は 2026-08-18 の**回顧**で一度測っている。
  市場残差 +0.87〜+1.18pt は本物だが**複勝回収74%止まり**
- 「失敗原因が今回消えるか」は 2026-08-31 の**相手のその後の再評価**で一度測っている。
  素の効果は実在したが**対照群が機構を否定**した
- ⚠ よって②に着手するなら、**上の2件と何が違うのか**を1行で書けること

🔑 **今回の違いは明確にしておく。**
> 個別レースの事後説明を作るのではなく、**予測時点で定義した「市場評価と過去情報の構造的なズレ」を
> 多数レースで OOS 検証する。**

---

## Phase 3 の基本フロー

```
Phase 1  市場 + AI モデル構築
   ↓
Phase 2  AI内部の能力分解  →  終了
         「能力をさらに分解すれば市場を超えられる」方向は閉じる
   ↓
Phase 3  市場の誤差が発生する条件を検証
   ↓
   ① 新情報                    ② 市場の解釈ミス
   ↓
   仮説カードを事前登録（中止基準・予算まで書く）
   ↓
   X単独 / Market / Market + X / Market + AI / Market + AI + X
   ↓
   AI強気 / 一致 / AI弱気（乖離方向別 OOS）
   ↓
   市場残差との関係（主指標）
   ↓
   選抜期A で決めて検証期B で評価（持ち越し＝一次基準）
   ↓
   年跨ぎ・別期間で再現
   ↓
   実オッズで ROI 確認  ← 「必要なら」ではなく必須。
                          残差だけではエッジの主張にしない（North Star #9）
```

---

## 再提案しないこと（§17②）

- 展開・隊列の精緻化／シミュレーション（三連複 +0.35%・必要 +33.3%）
- ペース × 脚質の細分化（完璧なペースでも +0.0007）
- コース one-hot・コース物理量の**本体投入**（-0.0003 / -0.0025）
- 市場アンカーを外す・弱める（3回否定）
- 人気帯・オッズ帯・乖離・主因・券種・セグメントでの**絞り込み**
- 資金配分・掛け金の最適化（総期待値は EV の加重平均。算数で動かない）
- EV = AI確率 × オッズ を買い目の根拠にする（3回否定・本番8,546頭で回収 57.9%）
- 特徴量を増やすこと自体（129特徴量 vs 市場アンカーのみ ＝ **+0.0009**）

**着手の条件**：上に触れる提案は「前回と**データ・機序・測り方のどれが**変わったか」を
1行で書けること。書けないなら着手しない。

---

## 最初の1歩（レビュー後に着手する順）

1. ~~**Phase 1b** を回す~~ ✅ 2026-09-16 完了（`HORSE_ABILITY_DESIGN.md` §10）
2. ~~`noryoku/CRITERIA_PHASE2.md` に Phase 2 の採用・中止基準を書く~~ ✅ 完了
3. ~~**感度検査**（既知の因子構造を注入して検出できるか）~~ ✅ 対照群B が k=2 を返して通過
4. ~~Phase 2 を測る~~ ✅ 完了（`noryoku/RESULTS_PHASE2.md`）
5. ~~Phase 2 を正式に閉じる~~ ✅ 2026-09-17 完了（本ファイル上部）
6. **次は Phase 3（新）＝ 市場評価が結果に対して系統的に外れる条件の検証**。
   着手の順序は固定:
   - **(a) 仮説カードを1枚書く**（中止基準・予算・assert 関数名まで空欄を残さない）
   - **(b) ②なら3条件を満たしているか確認**（X は変数形か／レース内で変化するか／持ち越しが一次基準か）
   - **(c) 対照群B を本データを見る前に通す**（既知の効果を注入して検出できるか）
   - **(d) 測る**
   ⚠ ①（新情報）は**前提条件2つ**（継続取得できるか／公開時刻とオッズの時間関係を見られるか）が
   揃うまで着手不可。現状は後者が満たせない

⚠ 1〜4 のどこでも、**本番モデル・買い目ロジック・表示は変更しない。**
