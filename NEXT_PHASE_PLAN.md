# NEXT_PHASE_PLAN — 「今回のレースで発揮できる能力」の推定

設計指示書 §20 Step 3 への回答。**本番コード・モデルには一切触れていない。**
前提の棚卸しは `HORSE_ABILITY_DESIGN.md` / `COURSE_ABILITY_MAP.md`。

---

## 0. 先に、この計画の見込みを正直に書く

設計指示書の狙い（「この馬は強いか」→「**今回の条件でその強さを発揮できるか**」）は、
これまで**部分的に4回測られていて、4回とも AUC では届かなかった**。

| 測ったもの | 結果 |
|---|---|
| コース適性に専用の学習枠を与える2段階モデル（2026-08-04） | **-0.0025** |
| 道悪 × 馬の型（スピード/スタミナ）の交互作用5列（2026-09-02） | **-0.0008**（道悪だけに絞っても -0.0007） |
| 展開・位置取りを完璧に予測できた場合の天井（2026-08-27） | ペース **+0.0007** / 事前予測できる位置取り **+0.0001** |
| 会場one-hot を本体に足す（2026-09-15②） | **-0.0003** |

🔑 いずれも「条件×能力の交互作用は実在するが、**木は既にそれを作れる状態にあり、
明示的に手渡しても上乗せがゼロ**」という同じ結論に着地している。

したがってこの計画は「効くはずだからやる」ではなく、
**「どこまでなら違う結果になりうるか」を1つずつ潰す**設計にする。
各 Phase に**中止基準**を置き、**通らなければそこで閉じる**。

⚠ 指示書は「目的は AUC を上げることではない」と言うが、
本プロジェクトの実測では**AUC が動かないものは回収率も動かない**（2026-08-18 ほか）。
AUC を目的にはしないが、**AUC が動かないものを回収率で救おうとしない**。

---

## 1. 共通のルール（全 Phase に適用）

| | |
|---|---|
| 事前登録 | 数字を見る**前**に採用基準・中止基準を `noryoku/CRITERIA_*.md` に書く。書いていない指標を後から採用根拠にしない |
| 感度検査 | 本データを見る前に、**既知の効果を注入して検出できるか**を確かめる（対照群B）。通らなければ本データを読まずに中止 |
| 対照群 | プラセボ（ラベルのシャッフル）だけで安心しない。「本物があっても0になる設計か」を別に問う |
| ハーネス | 本番同型（134列・residual・**ドリフト注入ON**・同一の行順）。A/B は**同じハーネス**で回す |
| 窓 | 3窓 × 3シード。1窓だけ良い結果は採らない |
| 別実装での突合 | 新しい量は**本番パイプラインで**作って値を突き合わせる。検証用スクリプトの数字を採用根拠にしない |
| 本番モデル | **設計レビューが終わるまで書き換えない**（指示書 §20） |

### 採用基準（AUC ゲート）— これを満たさなければ実装しない
1. 3窓すべてでプラス
2. 3窓平均 **+0.002 以上**
3. シード std の **2倍以上**
4. Brier / LogLoss も同方向

### 回収率ゲート（AUC ゲートを通ったものだけ）
5. OOS 1,500レース以上・実オッズ・実配当
6. 日ブロック bootstrap の **95%CI 下限が 100% を超える**
7. 上位3本の払戻を抜いても 100% を保つ
8. 前半・後半に割っても符号が変わらない

> 🔑 6〜8 を満たしたものは**このプロジェクトでまだ一度も無い**。
> 最良は「高自信レースの軸・単勝 95.9%（N=190）」で、大規模では 93.4% が天井。

---

## 2. Phase 一覧

### Phase 0 — 測れる土台にする（モデル変更なし）

| | |
|---|---|
| 仮説 | 134列には死んだ列・重複した列があり、そのままでは「軸を足した効果」を測れない |
| 使うデータ | `history.db` 全期間。`horse_features.csv` を再生成（15分） |
| やること | ① 死んだ4列（`f_early_speed` / `f_is_debut` / `f_same_course_rate` / `f_course_stamina_demand`）が学習データ全体でも定数・ゼロ gain か確認 ② 相関 >= 0.90 の列対を列挙 ③ 学習/推論パリティ違反（`f_track_cond` 常に0・`body_weight` 欠損）の影響量を測る |
| 評価 | 列を削っても AUC が **-0.001 以内**に収まるか（＝本当に死んでいるか） |
| 採用 | 死んでいると確認できた列だけ削る。**削っても効果は出ない**（削減は測定の見通しのため） |
| 中止 | `f_early_speed` が学習データで定数でなければ、この棚卸しが間違っている → やり直し |
| 見込み | ほぼ確実に通る。得られるのは精度ではなく**次の Phase の測定可能性** |

### Phase 1 — 能力軸がデータから出てくるか（人が型を決めない）

| | |
|---|---|
| 仮説 | 馬側の列（79列）は、人が名前で付けた15分類ではなく、**データ駆動の少数の因子**で説明できる |
| 使うデータ | Phase 0 の CSV。**その馬のレース以前の情報だけ**（リーク防止は既存パイプラインが担保） |
| 方法 | 馬側の列に因子分析／PCA。因子数は固有値・交差検証で決める（**手で決めない**）。因子スコアを特徴量として本番同型で A/B |
| OOS | 検定窓 2026-07-01〜（本番と同じ）＋ 2窓 |
| 指標 | AUC / Brier / LogLoss |
| **採用基準** | §1 の 1〜4 |
| **中止基準** | ① 因子が2つ以下（＝実質1軸で、分解する意味がない） ② A/B が 3窓平均 +0.002 未満 → **Phase 2 に進まない**  |
| 既知 | 手で切った7軸では PCA 第1主成分 39%・軸間相関の中央値 0.27 で、**1軸ではない**。ただし軸内の重複は大きい（`f_speed_fig_avg × max` = +0.93） |
| 見込み | 🟡 **軸が出ること自体は期待できるが、AUC が動く見込みは低い。** 木は既に元の列を全部見ているので、因子は情報の言い換えになる可能性が高い |

### Phase 2 — 「今回の条件で発揮できるか」（Phase 1 が通った場合のみ）

| | |
|---|---|
| 仮説 | 能力軸 × 今回の条件（コース・距離・馬場・展開）の交互作用を**明示的に**渡すと、木が自力で作るより良い |
| 方法 | Phase 1 の因子 × `COURSE_ABILITY_MAP.md` §2-2 の「✅測れる」列（初角距離・勾配・コーナー形状・芝スタート）の交互作用を**5列以内**で作る |
| **採用基準** | §1 の 1〜4 |
| **中止基準** | ① Phase 1 が通らなかった ② 3窓平均が +0.002 未満 ③ **条件を満たすレースだけに絞っても**改善しない（2026-09-02 の道悪と同じ形） |
| 既知 | 同型の試みが 3回とも -0.0003〜-0.0025。**「何が今回違うのか」を1行で言えなければ着手しない** |
| 見込み | 🔴 低い。差分は「軸をデータ駆動で作った点」だけ |

### Phase 3 — 説明（AUC を上げる話ではない・独立して進めてよい）

| | |
|---|---|
| 目的 | 指示書の「総合能力1位だが今回は評価を下げる、を説明できる形にする」 |
| 現状 | `shap_explain.py` が `ability_margin` を15カテゴリに分解済み。画面に📊で出ている |
| やること | カテゴリを `HORSE_ABILITY_DESIGN.md` の仕分け（馬能力 / 今回の条件 / 掛け合わせ / 過去の市場）に**貼り替える**。表示のみ |
| **採用基準** | 「なぜこの馬を下げたか」が1行で読めること。**予測値を1ミリも変えないこと** |
| **中止基準** | 予測値・買い目が変わったら差し戻す |
| 既知 | 🔴 **SHAP の内訳から「AIを信じてよいか」は読めない（-0.0038・2026-08-27）。** 説明であって選別ではない |
| 見込み | ✅ 通る。ただし回収率には影響しない |

### Phase 4 — Web情報（指示書 Gate 1〜7）

| | |
|---|---|
| 現状 | 🔴 この環境は egress proxy で**個別ページに到達できない**（`jra.go.jp` も `radionikkei.jp` も実測で EGRESS_BLOCKED）。使えるのは WebSearch の要約だけで、要約は複数の日付が混ざる |
| 🔴 決定的な制約 | **過去分が存在しない。** 学習データが作れない（North Star #3）。後から結果を見て記事を取得するのは指示書自身が禁じている |
| したがって | 層1（当日の馬場＝その日すでに終わったレースの結果から取る）は**予想に繋がっている**。層2（web の記録）は `data/web_notes/` に**記録のみ**。`src/utils/web_notes.py` が出典と信頼度のない値を弾く |
| 順序 | 記録 → 溜まる → **基準を先に書く** → 測る → 通れば繋ぐ（パドック入力と同じ） |
| **中止基準** | 記録が `official` で N=1,000 に達するまで**特徴量化しない** |
| 「本気度」の扱い | 指示書どおり**明示情報と推測を分ける**。`source` と `confidence`（official / reported / inferred）を必須にし、**特徴量候補にしてよいのは official だけ** |

### Phase 5 — 回収率ゲート

Phase 1〜2 のどれかが AUC ゲートを通った場合のみ実施。§1 の 5〜8。
通らなければ**実装しない**（2026-07-27 の「基準未達でも入れる」で一度痛い目を見ている）。

---

## 3. 依存関係と打ち切り

```
Phase 0（土台）──┬─→ Phase 1（軸）──→ Phase 2（交互作用）──→ Phase 5（回収率）
                 │        ↑ここで止まる可能性が最も高い
                 ├─→ Phase 3（説明）      ← 独立。AUCとは無関係
                 └─→ Phase 4（web・パドック）← 時間が解決するまで記録のみ
```

**Phase 1 が中止基準に触れたら、Phase 2 は実施しない。**
その場合に残るのは Phase 0（掃除）・Phase 3（説明）・Phase 4（蓄積）で、
いずれも**回収率を上げない**。それがこの計画の最も起こりうる結末であり、
先に書いておく。

---

## 4. 再提案しないこと（理由が新しくない限り着手しない）

指示書自身が「既に否定された方向は理由なく再提案しない」と定めている。

- 展開・隊列の精緻化／シミュレーション（三連複 +0.35%・必要 +33.3%）
- ペース × 脚質の細分化（完璧なペースでも +0.0007）
- コース one-hot・コース物理量の**本体投入**（-0.0003 / -0.0025）
- 市場アンカーを外す・弱める（3回否定）
- 人気帯・オッズ帯・乖離・主因・券種・セグメントでの**絞り込み**
- 資金配分・掛け金の最適化（総期待値は EV の加重平均。算数で動かない）
- EV = AI確率 × オッズ を買い目の根拠にする（3回否定・本番8,546頭で回収57.9%）
- 特徴量を増やすこと自体（129特徴量 vs 市場アンカーのみ = **+0.0009**）

**着手の条件**：上の項目に触れる提案は、
「前回と**データ・機序・測り方のどれが**変わったか」を1行で書けること。
書けないなら着手しない。

---

## 5. 最初の1歩（レビュー後に着手する順）

1. **Phase 0** を回す（`build_training_data` 15分 → 死んだ列・重複列の確認）
2. `noryoku/CRITERIA_PHASE1.md` に Phase 1 の採用・中止基準を書く
3. **感度検査**（既知の因子構造を注入して検出できるか）を本データの前に通す
4. Phase 1 を測る

⚠ 1〜4 のどこでも、**本番モデル・買い目ロジック・表示は変更しない。**
