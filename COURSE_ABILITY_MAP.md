# COURSE_ABILITY_MAP — コースの物理特性と馬能力の対応

設計指示書 §20 Step 2 への回答。**本番コード・モデルには一切触れていない。**

> 🔴 **最初に読むこと。** 「コース情報を本体モデル（3着内予測）に足す」方向は
> **4回測って4回とも効かなかった**。この文書は仮説の一覧だが、
> 既に閉じている扉を再び叩かないための境界線を先に引く。

| いつ | 何を試したか | 結果 |
|---|---|---|
| 2026-08-04 | コース20列に専用の学習枠を与える2段階残差モデル | **AUC -0.0025**（Stage A単独 0.7548 → 0.7523） |
| 2026-08-06 | 当日の先行レースから馬場バイアスを算出して特徴量化 | **AUC -0.0005** |
| 2026-09-15② | 会場one-hot を本体134列に足す | **-0.0003**（3窓） |
| 2026-09-15② | P(先頭)・隊列を本体に足す | **-0.0006**（3窓） |
| 2026-09-15② | 市場残差に「会場×馬場×実距離」の固定オフセットがあるか | **無い**。前期→後期で持ち越さない（ρ=+0.158, P=0.20）。上位セルは符号反転 |

🔑 決定的な対比が1つある。
**前半600m（市場アンカー無し）はコースで強く決まり、年をまたいで持ち越す（ρ=+0.819, P=0.0005）。
一方その同じコース情報は市場残差には一切出ない。**
＝ **コースは走りを決めるが、市場はそれを完全に値付けしている。**

---

## 1. コースDBに何が入っているか（`data/course_starts.json` 第2版・127組）

| フィールド | 充足 | 出典の質 |
|---|---|---|
| `start_to_first_corner_m` | ok **95** / needs_check 28 / merge_ambiguous 3 / none 1 | **A**=JRA公式本文 8件 / **C**=資料の表 87件。推測(X)は0件 |
| `straight_distance_m` / `course_elevation_m` / `one_lap_distance_m` / `turn_direction` | ほぼ全件 | 会場×馬場の定数 |
| `slope_start`（発走直後の勾配） | 上り/下り/平坦 | |
| `course_variant`（内回り/外回り/直線） | | 2026-09-15 に407レースの取り違えを修正 |
| `turn_shape`（spiral 等） / `first_corner`（1C〜4C/NONE） | | |
| `turf_start` | **8組** | 京都ダ1400・阪神ダ1400/2000・中京ダ1400 ほか |
| `start_location`（向正面・正面側…） | 79/127 | |

実レース（history.db 12,017R）のカバー率 **98.8%**。残りは全部**障害**の距離。

---

## 2. 物理特性 → 馬能力の対応（仮説と、実測できている部分）

### 2-1. 既に実測できている唯一の経路：**コース → ペース**

```
前半600m の残差を (会場, 馬場, 実距離) ごとの定数で説明すると      97%
その定数が年をまたいで持ち越すか（2025 → 2026・重複なし）    ρ=+0.819 (P=0.0005, n=25)
オフセットの std 0.24秒（残差 std 0.83秒の29%）
```

未見コース（leave-one-course-out）への外挿は、**スタート→初角の1列だけで
到達可能な上限の 21.6%** を取る。

```
A 会場one-hotのみ            0.8459s
B + 物理特徴41列             0.8306s   A比 -0.0153s
  うち「スタート→初角」1列だけ            -0.0241s   ← 41列全部より良い
  会場×馬場の定数だけ（直線長・高低差・1周・回り） +0.0162s   ← 悪化
注入 真のオフセット（到達不能な上限）        -0.1114s
```

🔑 **直線長・高低差・1周長は `venue` one-hot が既に持っている情報**なので、
足すと悪くなる。効いているのは**発走地点に固有の量**だけ。

### 2-2. 仮説（未検証。Phase で測る対象）

| 物理特性 | 関係すると考える馬能力 | なぜそう考えるか | いま測れるか |
|---|---|---|---|
| **スタート→初角距離** | 脚質（位置取り）・ゲート速度 | 短いほど内枠・先行有利。実際にペースを最もよく説明する1列 | ✅ 測れる（DB 95件・`f_pos_avg_3` と `f_post` がある） |
| 発走直後の勾配 `slope_start` | 初速・パワー | 上りスタートは前に行きづらい | ✅ 測れる |
| 直線長 | 瞬発（上がり）vs 持続 | 長い直線は末脚が届く | 🟡 測れるが**会場one-hotと重複**（2-1で悪化側） |
| 坂の高低差 | スタミナ・パワー | ゴール前の坂で止まる／止まらない | 🟡 同上 |
| コーナーのタイト度 `turn_shape` | 器用さ・コーナー通過位置の変動 | `f_corner_position_change`（gain 0.63%）が対応する列 | ✅ 測れる |
| **芝スタートのダート（8組）** | ダッシュ力・芝適性 | 芝で加速してから砂に入る。人間の予想では定番 | ✅ 測れる（`f_dirt_turf_start` gain 0.57%・既に本体に入っている） |
| 内回り/外回り | 器用さ・持続 | 同じ距離で全く別のレース | 🟡 `'両'`（同距離で内外どちらも施行）は距離から決まらず**解決しない** |
| 一周距離 | — | 会場定数。2-1 で悪化側 | ❌ 使わない |

### 2-3. 掛け合わせは既に実装されている

`f_course_fit_score` = コースの要求（`f_course_stamina_demand` / `f_course_speed_demand`）
× その馬の `f_stamina_score` / `f_speed_score`。2026-08-04 導入。

```
f_course_fit_score        gain 0.55%
f_course_speed_demand     gain 0.60%
f_course_stamina_demand   gain 0.00%   ← 分割に一度も出ない
```

🔴 **要求度の片側（スタミナ）は木が一度も使っていない。**
「要求プロファイル × 能力プロファイル」という形そのものは既にあり、
その取り分は小さい。**同じ形を作り直す提案はしない。**

---

## 3. 何を実測で検証できるか

| # | 問い | 測り方 | 既知の制約 |
|---|---|---|---|
| C-1 | スタート→初角距離は**枠順の有利不利**を説明するか | 初角距離 × 枠 の交互作用を市場残差で測る | 🔴 2026-09-14 に「初角距離で枠バイアスが読める」を**否定**済み（ρ -0.647→-0.278, P=0.137。出典 official のみなら -0.039）。⚠ ただし当時は N>=300 のセルが30組。第2版DBで**セル数が増えた**ので再測の余地はある |
| C-2 | 芝スタートのダート8組で、**芝実績のある馬**が有利か | 交互作用列を1本足して本番同型A/B | 🟡 対応する列は既にある（`f_dirt_turf_start`）。馬側との掛け算が無いだけ |
| C-3 | 初角距離 × 脚質（`f_pos_avg_3`）の交互作用 | 同上 | 🔴 2026-08-27「展開系29列を全部抜いても -0.0001」がある |
| C-4 | コースDBの粒度を上げると**ペース予測**が良くなるか | 既存の leave-one-course-out | ✅ ここだけ**通っている**（-0.0241s） |

🔑 **C-1〜C-3 はいずれも「本体モデルに足す」話で、
冒頭の表のとおり4回とも -0.0003〜-0.0025 だった。**
測るなら、事前に「これまでと何が違うのか」を1行で言えることが条件（NEXT_PHASE_PLAN.md §4）。

---

## 4. コースDBの用途（2026-09-15② で確定・変更しない）

1. **ペースモデル**（前半600mの予測）— ここだけ効果が実測されている
2. **説明**（なぜこの馬を上位に採ったかの提示）
3. **観測の薄い組への外挿**（未見コースのペース）

**本体モデル（3着内）の精度向上は用途から外す。**
指示書の「盲点が見つかったら本体に戻す」は、市場残差に偏りが無いと確認済みのため発動しない。

---

## 5. まだ埋まっていないコースデータ

| 欠けているもの | 件数 | 影響 |
|---|---:|---|
| スタート→初角が `needs_check` | 28組 | 外挿の検出力が落ちる |
| `merge_ambiguous`（本線合流までしか分からない） | 3組 | 東京芝1800/2000・札幌芝1500。🔴 **旧ファイルはここで本線合流の100mを初角距離として返していた**（2026-09-15③で修正） |
| 障害コース | 実レースの1.2% | 予想対象外なので影響なし |

優先順位は `docs/course_data_wanted.md` に記録済み。
