# ① の残り2情報源を実地調査した（**測定していない**）

日付: 2026-09-25 / ネットワークポリシー: **Full**

## これは何の記録か

`websig/RESULTS.md`（kayochinkeiba の能力指数）を閉じた時点で、direction ①
（＝予測時点でまだ市場に入っていない新情報）の候補として残っていたのは
**2件だけ**だった。その2件について、**測定予算を使う前に「データ量と粒度が
足りるか」を実物で確かめた**記録。

🔴 **測定していない。** 書いてよいのは「**測定予算を使う価値が無いと分かった**」まで。
「効かないと分かった」とは書かない（2026-09-18 の P3-02 と同じ扱い）。

---

## ① koukakukeiba.jugem.jp（調教タイム）→ **閉じる**

事前見積りは 🟡 medium だった。調教は 2026-08-16 に
「丸ごと欠けている唯一の次元」と記録し JRA公式ルートを打ち切った領域なので、
①の中では最も筋が良いと見ていた。

### 🔴 決定的: このブログは「臨時サイト」で、過去分が1本も取れない

| 確認したこと | 実測（2026-09-25） |
|---|---|
| `<title>` | **DATA KEIBA REAL SIDE　臨時サイト** |
| 一覧に出るエントリ | トップ 28件 / RSS 48件 ＝ **直近3日ぶんだけ** |
| 月別アーカイブ | トップが張っている `?month=202608` すら **HTTP 404** |
| 古い記事に直接 | `?eid=5000` → **404**（古い eid は消えている） |
| 本サイト `koukaku.joywork.jp` | **同じブログ**（同一 title・同一 eid・同じ3ヶ月）。JUGEM の独自ドメイン。深いアーカイブは**そちらにも無い** |
| robots | `User-agent: * / Allow: / Crawl-delay: 10`（Claude の名指しなし） |

🔑 **kayochin と決定的に違う。** kayochin は sitemap で 2016-12 まで遡れたので
「溜まるまで18ヶ月待つ」が不要になった（811記事を30分で取得）。
jugem は**過去分ゼロ**なので、**今から蓄積するしかない**。
→ North Star #3（データなしの特徴量追加をしない）に正面からぶつかる。

### 🔴 もう1つ: タイムは「1頭/会場」ではなく **1頭/開催日** だった

CLAUDE.md には「タイム付きは注目馬1頭/会場」と書いてあったが、実物を数えると
**記事1本につき1頭**（その日の全会場を1記事でまとめている）。**訂正する。**

```
記事                     タイム付き   印のみ（一覧の頭数）
9月20日[調教注目馬]            1            27
9月21日[調教注目馬]            1            29
9月22日[調教注目馬]            1            14
```

タイムの中身自体は本物（例: 「中山11R 7レガレイラ ／ 9/6(日)美浦坂路 ／
タイム 52.1 ／ ラップ 14.2-13.5-12.5-11.9」）。問題は量と形。

### 算術上の天井

| | |
|---|---|
| 到達可能な N（タイム） | **1頭/開催日**。JRA の開催日は約106日/年（実測: 3.75年で399日）→ N=390 まで**約3.7年**、N=2,000 まで**約19年** |
| 標本の性質 | 「注目馬」＝**筆者が選んだ1頭**。無作為標本ではない |
| 🔴 レース内正規化が成立しない | 1レースに1頭しか居ないので **順位は常に 1.0・z は sd=0 で 0.0**。`src/features/web_signals.py` の rank/z 設計がそのまま使えない。生の坂路タイムを渡すしかないが、美浦/栗東・坂路/ウッド・馬場状態・追い方で意味が変わるので横比較できない |
| 🔴 検出力 | 3着内率のベース22%で N=390 なら SE = √(.22×.78/390) = **2.1pt**。信号ライン +2pt は **SE 以下** ＝ North Star #11 の「SE より小さい閾値は厳しい基準ではなく何も判定できない基準」 |
| 印（タイムなし・14〜29頭/日）の側 | N=2,000 に 8ヶ月〜1.4年（過去分ゼロなので全部待ち）。中身は **websig と同型**（人が選んだ推し）。websig は 134,874頭（345倍）・全レース全馬・連続量という**あらゆる点で強い条件**で測って、**回収率 79.7%→79.1% で悪化**して閉じた。jugem の印は二値・疎・母集団も小さい＝**あらゆる点で弱い** |

**判定: 測らずに閉じる。**

### ⚠ 副産物（測っていない・記録のみ）

RSS の「今週の重賞出走予定馬と**外厩情報**」は、重賞の**全出走馬**について
`枠/番/馬名/性齢/斤量/調教師/外厩情報` の構造化テーブルを持っている
（例: ノーザンファームしがらき / 天栄 / 社台ファーム鈴鹿 / 大山ヒルズ）。
外厩は DB に無い次元で、形も良い。🔴 ただし **重賞のみ（週1〜2レース）**で、
①**過去分は同じ404の壁**で取れない ②重賞は 2026-08-18 に
「**OPだけAIの上乗せがマイナス（−0.0039）**」と測った領域
＝ データが存在する母集団が、記録が「磨いても取れない」と言っている母集団と一致する。

---

## ② bloodline-trackbias（トラックバイアス予想）→ **閉じる**

事前見積りは 🔴 low（馬場バイアスは4回否定済み）。

### ✅ 到達性・アーカイブは jugem より良い（ここは訂正）

| | |
|---|---|
| apex `bloodline-trackbias.work` | **Connection reset**（157.7.174.95） |
| **`www.bloodline-trackbias.work`** | **200**。DNS は `hatenablog.com` ＝ はてなブログの独自ドメイン |
| robots | `Disallow: /api/ /draft/ /preview` のみ。**Claude の名指しなし** |
| sitemap | `sitemap_index.xml` → 月別 **89本・2019-05 まで遡れる**。history.db の 2023-01〜2026-09 を**完全にカバー** |
| 記事数 | 2026-09 は 13本 ＝ ほぼ1開催日1本 |
| 公開時刻 | **前日 19:00 / 20:00**。本番生成は 19:06 なので前日夜には半分間に合わないが、**2026-09-25 に作った朝8時 refresh の経路で拾える**（＝タイミングはもう障害ではない。CLAUDE.md の「間に合わない」は訂正） |

🔑 つまり **①で唯一、過去分が取れて robots も規約も通り、経路も用意できている情報源**。
それでも閉じるのは、中身の粒度と、既に測り終えている天井のため。

### 🔴 粒度: 「レース単位」ではなく **会場×馬場単位・1日4セル**

CLAUDE.md には「レース単位のトラックバイアス予想」と書いてあったが、実物は
**1記事に 2会場 × 芝/ダート = 4セル**で、レースごとの記述は無い。**訂正する。**

実物（2026-09-19 20:00 公開・9/20 中山/阪神ぶん）の構造:

```
中山 芝  想定雨量10〜100㎜ / 想定馬場やや重〜不良 / 想定時計 −1.0〜＋3.0秒
        枠   … 内〜フラットと想定（大量の雨で内も外も変わらなくなるようだと内枠有利）
        直線 … フラット〜やや外と想定
        前後 … 前〜展開次第と想定
中山 ダート / 阪神 芝 / 阪神 ダート … 同じ3軸
```

### 算術上の天井

| | |
|---|---|
| セル数 | 4セル/日 × 約390開催日 = **約1,560セル** |
| 🔴 レース内で一定 | 枠・直線・前後はいずれも**その日その馬場で共通**。2026-09-15 に実測で確定した通り、1レースで3着内は必ず3頭なので**レース内の残差の和が構造的に≈0に固定**され（実測 std 16.4pt／独立なら155pt）、**レース内で一定の属性でまとめると市場が何をしていようが0に潰れる** |
| 馬番と掛ければレース内で変わる | それは **`f_bias`／当日馬場バイアスそのもの**。2026-08-06 に当日の先行レース結果から算出して **−0.0005**、2026-08-26 に `f_bias` を繋いで **−0.0005**、2026-08-27 に馬場状態の反映を3段階で否定、2026-09-15 に市場残差の発走地点偏りを否定＝**4回** |
| 天井は既に測ってある | 2026-08-17 の129軸総当たりで2年再現したのは `f_post`（枠順＝当日の馬場バイアス）**1軸のみ**（r≈+0.008〜0.010）。それを共通乱数にしたモデルを厳密評価すると三連複の確率が **+0.35%**。必要なのは控除率25%＝**+33.3%** で **約100倍足りない** |
| 🔴 値が条件付きレンジ | 「内〜フラットと想定」「大雨が降れば内枠有利」。固定した連続量 X に落とすには**どのシナリオが実現したかを選ぶ**必要があり、**実現後に選べばリーク**（2026-07-30 member_level と同型）、実現前に選ぶには天気予報が要る（記事が持っていない情報）。リークなしで落とせるのは**レンジの中央値**だけで、そこまで潰すと「今日は内が伸びやすいか」という**1日4個の粗い量**になる |

**判定: 測らずに閉じる。** 馬場バイアスを、**4回否定した時より粗い代理変数**
（人の散文のレンジ中央値）で5回目を測る理由が無い。

### ⚠ 1つだけ生き残る用途（別テーマ・今回は繋がない）

**想定馬場状態・想定時計は「前日夜に分かる馬場の予報」**で、
`src/scraper/going.py` が持っている穴——**R1 が終わるまでは「良」しか返せない
／前日夜の生成では翌日の馬場が不明**——を埋める唯一の候補。
🔴 ただしこの上限も測ってある: 2026-08-27 に本番の `calc_all` を4通り回して
**軸が入れ替わるのは 2.5% のレースだけ**（勝率の変化は中央値 0.03pt）。
繋いでも「同じ予想が4つ並ぶ」。

---

## ① の状態

```
web全体                              → Full にしたが、robots/規約/量の壁は残る
 └ ①の候補として使えた 3件
    ├ kayochinkeiba   → 測って不採用（websig/RESULTS.md・回収率 79.7→79.1）
    ├ koukakukeiba-jugem      → 本記録で 測らずに閉じる（過去分ゼロ・1頭/日）
    └ bloodline-trackbias     → 本記録で 測らずに閉じる（会場×馬場4セル・馬場バイアスは4回否定・天井が100倍足りない）
```

🔴 **これで ① は測れる候補を使い切った。**
⚠ 「web情報は無意味」ではない。**測ったのは1件・調査したのは3件**で、
「あらゆる情報」「集約」「信頼性の学習」は**3つとも未検証**のまま
（`websig/RESULTS.md` の射程の記述をそのまま引き継ぐ）。
⚠ 新しい情報源を見つけたら、この2件を閉じた理由（**過去分があるか / レース内で
変わる量か / 天井が測られていないか**）をそのまま判定表として使える。
