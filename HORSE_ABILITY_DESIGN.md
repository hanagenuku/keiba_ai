# HORSE_ABILITY_DESIGN — 現在の馬特徴量の棚卸し

設計指示書 §20 Step 1 への回答。**本番コード・モデルには一切触れていない。**
測定スクリプトは `noryoku/inv1.py`（仕分け）・`noryoku/inv2.py`（能力軸）。

> ⚠ この文書は「どの列が何に依存しているか」の**仕分け**であって、
> 「その列が有用か」の評価ではない。有用性は gain 欄と NEXT_PHASE_PLAN.md で扱う。

---

## 0. 測り方（名前で分類していない）

`shap_explain.FEATURE_CATEGORY_MAP` の15分類は**説明表示用に人が付けた名前**で、
設計指示書が求める「馬能力 / 市場由来 / レース由来 / コース由来」とは別物。
名前から推測すると `f_course_*` のように**中身が逆のもの**を取り違える。

そこで本番と同じ経路（`calc_features_for_xgb` → `add_relative_features`）を
実データで回し、**入力を1つずつ摂動して動くかどうか**で仕分けた。

| 摂動 | 動いた列の意味 |
|---|---|
| A 同じレース内で馬を変える | 動かない列＝**馬の情報を含まない**（レース共通） |
| B 今回の会場・馬場・距離を差し替える | 動く列＝**今回の条件に依存**する |
| C 今回の人気・オッズを差し替える | 動く列＝**今回の市場に依存**する |
| D 過去走を空にする | 動かない列＝**その馬の履歴を通っていない** |

摂動は **2026年の実レース8つ（実データ・本番と同じ馬・同じ過去走）**に対して行った。
gain は本番モデル `data/xgb_fukusho_model.pkl`（134列・residual・val AUC 0.7846）の
`get_score(importance_type='gain')` を正規化したもの。

⚠ 「レース内で全馬同じ値」は**この8レースで**同じだった、という意味。
`f_weight_trend_avg` / `f_weight_last_diff` は馬体重が 0% の期間では全馬 NaN になり
一見レース共通に見えるが、値が入る期間では馬ごとに違う（6レースで測ったときは
誤って共通側に出た）。データ欠損と構造的な共通性は別物。

### 🔴 この測定で計測器を2回壊した（結果より先に書く）

| # | 壊れ方 | 出ていた嘘 | 気づいた経路 |
|---|---|---|---|
| 1 | 差分判定が素の不等号だった。`NaN != NaN` は常に False なので**「値→NaN に変わった」を見逃す** | 「過去走を消しても 87列しか動かない」（正しくは102列）。`f_pop_last` 等が"履歴を通らない"と誤分類された | 履歴由来のはずの列が静的側に並んでいた |
| 2 | `race` に `_pace_dist_cache` が生える（`engine.py:2507`）。浅いコピーで摂動すると**前のレースのペース確率を使い回す** | 「ペース確率はコースを変えても動かない」（実際は 中山芝1800 high 0.058 → 新潟ダ1200 high 0.537 と大きく動く） | 機序と矛盾したので直接呼んで確認した |

対策として `noryoku/inv1.py` に**感度検査**を入れた。
`f_recent` が過去走に・`f_same_course_rate` がコースに・`f_pace_prob_fast` がコースに
反応しなければ、結果を印字せず中止する（2026-08-25 D-2④ の「関門そのものが壊れていた」の再発防止）。

---

## 1. 仕分けの結果（134列）

```
レース共通（馬の情報を含まない）   10列   gain  4.3%
馬 × 今回の条件（交互作用）       45列   gain 33.2%
馬のみ（今回の条件に依らない）     79列   gain 62.5%
今回の市場に依存する列              0列   gain  0.0%   ← 特筆
```

### 🔑 いま「市場由来」の特徴量は**ゼロ列**

今回のオッズ・人気を差し替えても **134列は1つも動かない**。
市場は特徴量ではなく `base_margin`（残差学習の土台）としてだけ入っている。

ただし**過去走での市場評価**は3列ある（`f_pop_last` / `f_pop_avg` /
`f_beat_market_rate`、合計 gain **4.54%**）。これらは「その馬が過去に何番人気で、
人気を上回ったか」であり、North Star #1 が禁じる**今回の市場オッズではない**。
`f_pop_last` は単独 gain 2.08% で全134列中5位。

> 設計指示書は「馬能力DBと コースDBを別管理し、最後に掛け合わせる」と言うが、
> **市場は第3の入力で、いまの構造では特徴量側に一切混ざっていない**。
> ここは既に分離できている。壊さないこと。

### レース共通の10列（全馬で同じ値＝順位付けに効かない）

| 列 | gain | 中身 | 状態 |
|---|---:|---|---|
| `f_course_speed_demand` | 0.60% | 直線長から作る「速さの要求度」 | コースDB由来 |
| `f_class_level` | 0.67% | クラス | レース条件 |
| `f_track_cond` | 0.57% | 馬場状態 | 🔴 **推論時つねに 0.0（良）** |
| `f_course_hill_diff` | 0.62% | 坂の高低差 | コースDB由来 |
| `f_dirt_turf_start` | 0.57% | ダートの芝スタート | コースDB由来 |
| `f_pace_prob_fast` / `_slow` | 0.55/0.45% | 展開モデルの出力 | レース条件のみで決まる |
| `f_course_corner_tight` | 0.25% | コーナーのタイト度 | コースDB由来 |
| `f_course_stamina_demand` | **0.00%** | 坂から作る「スタミナ要求度」 | 🔴 **分割に一度も出ない** |
| `f_early_speed` | **0.00%** | 前半3F | 🔴 **常に 36.0 の定数** |

🔑 **レース内で全馬同じ値の列は、レース内の順位付けには原理的に寄与しない。**
効くとすれば「このレース全体の難易度」を通じた較正だけで、実測 gain も 4.3% と薄い。
それでも木が使うのは、`base_margin` の水準を条件ごとに補正しているため。

🔴 `f_early_speed` は**学習側も推論側も定数**。
学習は `build_training_data.py` が `race['first_3f'] = 0.0`（リーク防止・正しい）、
推論は出馬表に前半3Fが無く既定 36.0。つまり `float(0.0 or 36.0) = 36.0` で
**全行 36.0**。1列まるごと死んでいる。

### 分割に一度も出なかった4列 — **理由は4つとも別だった**（Phase 1b で確定・§10）

`f_is_debut` / `f_early_speed` / `f_same_course_rate` / `f_course_stamina_demand`

🔑 **164,915行の学習データで測り直すと、情報が無いのは1列だけだった。**
残り3列は**別の列と数学的に同じ値**で、木がそちらを使っているだけ。

| 列 | 正体 | 一致 |
|---|---|---|
| `f_early_speed` | **定数 36.0**（学習・推論とも） | 164,915 / 164,915 行 |
| `f_same_course_rate` | **`f_course_fukusho` とビット単位で同一** | **100.0000%**（最大差 0） |
| `f_course_stamina_demand` | **`f_course_hill_diff` / 4.3**（単なる線形変換） | r = +1.000 |
| `f_is_debut` | **`f_career_runs == 0` と完全同値** | 100%（24,488頭） |

🔴 **つまり「このコース得意か」が無視されていたのではない。**
同じ値が `f_course_fukusho`（gain 0.39%）として使われており、
`f_same_course_rate` は**名前が2つある1つの列**だった。
木にとって単調変換（/4.3）も同値なので、`f_course_stamina_demand` も同じ。

---

## 2. `f_course_*` の完全分離（設計指示書 Step 1 の名指し）

指示書の懸念どおり、`f_course_` で始まる列には**性質の違う3種類が混ざっていた**。

### (a) その馬のコース実績（＝馬能力側） 14列 / gain 6.77%

今回のコースを**フィルタ条件として使うだけ**で、値は馬の過去走。

| 列 | gain | 何を数えているか |
|---|---:|---|
| `f_agari_at_similar` | 0.85% | 似た直線長のコースでの最速上がり |
| `f_course_type_rate` | 0.66% | 同じコース類型での好走率 |
| `f_corner_position_change` | 0.63% | 3角→4角の位置変動 |
| `f_uphill_severity_rate` | 0.50% | 坂の厳しさが合う過去走での好走率 |
| `f_same_turn_rate` | 0.45% | 同じ回りでの好走率 |
| `f_agari_rank_at_type` | 0.45% | 同類型での上がり相対順位 |
| `f_straight_match` | 0.44% | 直線長クラスが合う過去走での好走率 |
| `f_tight_vs_spacious` | 0.40% | タイト/広いコースの得手不得手 |
| `f_course_coverage` | 0.40% | 同一コースでの経験走数 |
| `f_course_fukusho` | 0.39% | 同一コースでの複勝率 |
| `f_same_course_rate` | **0.00%** | 同一コースでの好走率 |
| `cl_f_course_fukusho_*` | 1.02% | 上記のレース内相対化2列 |

### (b) 今回のレースのコース情報（＝馬に依らない） 7列 / gain 3.05%

`f_dirt_turf_start` / `f_course_hill_diff` / `f_course_corner_tight` /
`f_course_speed_demand` / `f_course_stamina_demand` / `f_pace_prob_fast` / `f_pace_prob_slow`

**全馬で同じ値**。(a) と同じ `f_course_` 接頭辞なのに、レース内の順位を1ミリも動かさない。

### (c) 掛け合わせ（能力 × 条件） 11列 / gain 6.25%

`f_course_fit_score`（コースの要求 × その馬のスタミナ/スピード）・`f_style_course_fit` /
`f_pace_fit` / `f_style_total_fit` / `f_pace_x_style` と、その相対化6列。

> 🔑 **設計指示書が提案する「馬能力 × コース適性を最後に掛け合わせる」形は、
> 既に (c) として実装済み**（2026-08-04 導入）。新規の発明ではない。
> 現状の取り分は 11列で gain 6.25%、単独最大は `f_style_course_fit` の 0.80%。
> しかも 2026-08-04 に**専用の学習枠を与える2段階モデルは AUC -0.0025 で否定**されている。

---

## 3. その他の分類（設計指示書の軸に合わせて）

| 区分 | 列数 | gain | 備考 |
|---|---:|---:|---|
| PLレーティング（実力） | 5 | **7.02%** | 群として最大。⚠ `h['history']` を**通らない**（`_PL_RATINGS` から馬名で引く別経路） |
| 距離適性 | 11 | 6.61% | `f_stamina_score` / `f_speed_score` は (c) の入力でもある |
| 騎手・厩舎 | 7 | 6.53% | 履歴を通らない（辞書引き） |
| 過去の市場評価 | 3 | 4.54% | 上述。今回の市場ではない。**1列あたり 1.51% は全群で最大**（PL 1.40% / 騎手厩舎 0.93% / コース実績 0.48%） |
| その他（近走・スピード指数・位置取り・斤量・血統ほか） | 73 | 58.0% | |

### 過去走を消しても変わらない 32列 / gain 23.5%

馬の情報が `h['history']` 以外の経路からも入っていることを意味する。

- **PLレーティング5列** … `_PL_RATINGS`（馬名キー・週次更新）
- **騎手/厩舎7列** … `_jockey_dict` / `_trainer_dict`
- **スピード指数** … `speed_index_cache.pkl`
- 静的属性（`f_sex` / `f_age` / `f_weight_load` / `f_blood` / `f_post`）

🔑 **馬能力DBを作るなら、既に事実上2つある**（`h['history']` と 各種 pkl 辞書）。
新設する前に、この2経路のどちらに何が乗っているかを固定しないと、
同じ量が別実装で2つでき、片方だけ更新される（このプロジェクトで繰り返した事故）。

---

## 4. 能力軸は既存データで「分かれて」いるか（`noryoku/inv2.py`）

設計指示書は「1スコアにまとめず能力軸に分解する。ただし人間が型を決め打ちしない」と言う。
その手前の問いを先に測った——**いま持っている列は本当に別々の軸か**。
2026年400レース・4,399頭（欠損50%超と定数列を除く21列）。

```
軸どうしの相関（向きを揃えて標準化・軸内は列の平均）
                  速   持続   瞬発  スタ   脚質  コース  総合
スピード（絶対時計）  +1.00 +0.46 +0.66 +0.23 +0.08 +0.35 +0.35
持続（平均ペース）   +0.46 +1.00 +0.45 -0.30 +0.17 +0.27 +0.38
瞬発（上がり）     +0.66 +0.45 +1.00 +0.19 -0.11 +0.29 +0.40
スタミナ        +0.23 -0.30 +0.19 +1.00 -0.02 +0.16 +0.21
脚質（位置取り）    +0.08 +0.17 -0.11 -0.02 +1.00 +0.20 +0.10
コース形状対応     +0.35 +0.27 +0.29 +0.16 +0.20 +1.00 +0.50
総合レーティング    +0.35 +0.38 +0.40 +0.21 +0.10 +0.50 +1.00

軸間相関の |中央値| 0.27 / 最大 0.66
PCA 寄与率 39% 19% 16% 11% 7% 5% 4%   第1・第2で 57%
```

✅ **「強さ」という1軸の言い換えではない。** 第1主成分は39%しか説明しない。
特に**脚質は他のどの軸ともほぼ無相関**（|r| <= 0.20）、
**スタミナは持続と負の相関（-0.30）** で、独立した情報を持っている。

⚠ ただし**この軸の切り方は私が手で決めたもの**で、指示書が禁じた「人間が型を決め打ち」
そのもの。データ駆動の分解（全馬列に対する因子分析）は Phase 2 に置いた。

🔴 **軸内の重複は大きい。** `f_speed_fig_avg × f_speed_fig_max` が **+0.93**、
`f_same_turn_rate × f_straight_match` が **+0.89**、`f_speed_avg × f_late_speed` が +0.85。
「軸を増やす」前に、**同じことを言っている列を1本にまとめる**余地がある。

---

## 5. 未定義（いま列が存在しない）

| 指示書の要素 | 状態 |
|---|---|
| 当日の馬場状態 | 🔴 `f_track_cond` は**推論時つねに 0.0**。学習時だけ本物（良以外は 27.2%）。2026-09-15 に「その日すでに終わったレースから取る」経路を新設したが、R1〜R3 と前日生成は依然「良」 |
| 馬体重・増減 | 🔴 `body_weight` 充足 6.5%、2025-01〜2026-02 は **0%**。`f_weight_trend_avg` / `f_weight_last_diff` は大半 NaN |
| 調教・追い切り | 🔴 **存在しない**。JRA公式に機械可読なタイムが無いと確定（2026-08-16・実HTML4回）。netkeiba は有料 |
| 陣営コメント・本気度 | 🔴 存在しない。この環境は egress proxy で個別ページに到達できない（2026-09-15） |
| パドックの気配 | 🟡 `paddock_score`（0〜10・絶対評価）を 2026-09-02 に新設。**蓄積のみで予想に繋いでいない**。N=2,000 到達まで約3ヶ月 |
| 枠番 | 🔴 `horse_history.bracket` は 0%。`f_post` は馬番から計算している |
| 血統（母父） | 🟡 `sire` 充足 163,402行中 **1,362行=0.8%**。走数100以上の種牡馬は15頭 |

---

## 6. 指示書 §3 の能力軸に、いまの列を当てる

「既存能力 / 重複 / 未定義 / 新たに必要な能力軸」（§20 A）の対応表。
✅=十分な列がある / 🟡=代理はあるが直接ではない / 🔴=列が無い

### A. 基礎的な競走能力

| §3 の軸 | いまの列 | 判定 |
|---|---|---|
| スピード能力 | `f_speed_avg` `f_speed_max` `f_speed_last` `f_speed_fig_last/avg/max` `f_speed_score` ＋相対6列 | ✅ **最も厚い。ただし重複が大きい**（`f_speed_fig_avg × max` = +0.93） |
| 持続力 | `f_finish_time_avg` `f_time_diff_avg` | 🟡 タイム差で代理。「長く脚を使えたか」を直接見てはいない |
| 瞬発力 | `f_late_speed` `f_last1_3f` `f_last2_3f` `f_last3_3f` `f_agari_ability` | ✅ |
| スタミナ | `f_stamina_score` `f_optimal_distance` | ✅ |
| 最高パフォーマンス | `f_speed_fig_max` `f_speed_max` | ✅ |
| **安定性** | `f_pos_std_3`（位置取りのばらつき）だけ | 🔴 **着順・スピード指数のばらつきを表す列が無い**。新規に必要 |
| 成長・下降傾向 | `f_speed_trend`（1列）／`f_blood`（年齢とピークの差） | 🟡 薄い |

### B. 距離に関する能力

| §3 の軸 | いまの列 | 判定 |
|---|---|---|
| 短距離/マイル/中距離/長距離 適性 | `f_dist_fukusho`（**今回の距離帯**での複勝率）`f_optimal_distance` `f_dist_confidence` | 🟡 **距離帯ごとに独立した列は無い**。今回の距離に対する1つの値に潰れている |
| 距離延長への対応 | `f_stamina_x_extension` `f_dist_change` | ✅ |
| 距離短縮への対応 | `f_speed_x_shortening` `f_dist_vs_optimal` | ✅ |

### C. 脚質・レース運び

| §3 の軸 | いまの列 | 判定 |
|---|---|---|
| 逃げ/先行/好位/差し/追込 | `f_p_front` `f_p_mid` `f_p_back` `f_pos_avg_3` `f_last1〜3_pos3c` | 🟡 **3コーナー通過順の比率**であって「逃げ能力」ではない。5分類ではなく3分類 |
| 位置取りの安定性 | `f_pos_std_3` | ✅ |
| 前受け能力 | — | 🔴 無い |
| 外を回す形への適性 | — | 🔴 **無い。しかも作れない**（隊列表記 `corner_pass` は充足 4.1%） |
| 内で立ち回る能力 | `f_corner_position_change`（3角→4角の位置変動） | 🟡 近いが「内か外か」は見ていない |

### D. 末脚特性

| §3 の軸 | いまの列 | 判定 |
|---|---|---|
| 上がり性能 | `f_late_speed` `f_agari_ability` `f_agari_rank_at_type` | ✅ |
| 瞬間的な加速力 | — | 🔴 無い。**区間ラップが要る**（`lap_times` は 2025年以降のみ） |
| 長く脚を使う能力 | — | 🔴 無い（同上） |
| 早めに動く形への適性 | — | 🔴 無い |
| 直線だけの瞬発力勝負 | `f_agari_at_similar`（似た直線長での最速上がり） | 🟡 |

### E. コース形状への対応能力

§2 (a) の14列がここに当たる（gain 6.77%）。
⚠ `f_same_course_rate` は gain 0.00% だが、これは**`f_course_fukusho` と同一値だから**で、
「コース実績が効いていない」という意味ではない（Phase 1b・§10）。

### 🔑 新たに必要な軸（作れるもの / 作れないもの）

| 軸 | 作れるか | 必要なもの |
|---|---|---|
| **安定性**（着順・指数のばらつき） | ✅ **いま作れる**。`place` は100%・`f_speed_fig` も揃っている | なし |
| **距離帯ごとの独立した適性** | ✅ いま作れる | なし |
| **加速力・脚の使い方**（区間ラップ由来） | 🟡 2025年以降だけ | `lap_times`（2023-24は0%） |
| 前受け・外回し | 🔴 作れない | 隊列表記（充足 4.1%） |
| 当日の気配 | 🟡 蓄積中 | `paddock_score`（N=2,000 まで約3ヶ月） |
| 調教・陣営の意図 | 🔴 作れない | 到達手段が無い（§5） |

---

## 7. メンバーレベル（指示書 §9）— 分離は**既に大部分できている**

§9 は「絶対能力」と「今回のメンバー内での相対能力」を分けよ、と言う。

```
レース内で相対化された列（rl_* / cl_*）   43列   gain 38.0%
```

🔑 **相対化の列は134列中43列で、gain の 38.0% を占める最大の塊。**
`rl_f_recent_vs_field`（3.44%）と `rl_f_recent_rank`（3.36%）は全体の2位・3位。
つまり「今回のメンバーの中でどうか」は**既にモデルの中心**にある。

🔴 **一方で「前走のメンバーレベル」は 2026-07-28 に削除されている。**
`f_member_level_*` は相手馬の**そのレース以降**の成績を使っており、
これが「回収率261%のセグメント」の正体だった（North Star #8）。

> 線引き（2026-08-31 に確定）:
> **予測日より後の相手成績＝リーク ／ 過去走から予測日までの相手成績＝合法。**
> §9 を再実装するなら、この線を越えていないことの検査を先に書く。

⚠ 加えて 2026-08-31 に「過去走の相手がその後どうなったかで過去走を再評価する」を
測り、**素の効果は実在したが対照群が機構を否定した**（自分が負かした馬がその後
勝った場合も同じだけ効いた＝方向を持たない「レースレベル」の情報で、
既存の `f_recent`・θ・市場人気が吸収済み）。本番同型A/Bで **-0.0005**。

---

## 8. 画面表示の分離（指示書 §15）

§15 は「総合能力 / コース適性 / 今回条件適性 / メンバー比較 / Web情報 / 総合期待」を
別々に出せる構造を求めている。**材料は既にある。**

| §15 の欄 | 対応する既存の量 |
|---|---|
| 総合能力 | `ability_margin`（市場に依らないAI評価。`raw_margin − base_margin`） |
| コース適性 | §2 (a) の14列 → SHAP の「コース適性」カテゴリ |
| 今回条件適性 | §2 (c) の掛け合わせ11列 |
| メンバー比較 | `rl_*` / `cl_*` の43列（`f_cl_rank` は単独 gain 1位の 5.30%） |
| Web情報 | 🔴 まだ無い（記録のみ） |
| 総合期待 | `cal_prob` / `top3_prob` |

不足しているのは**分解の道具ではなく貼り替え**で、`shap_explain.py` の15カテゴリを
本書の仕分けに合わせ直せば §15 の形になる（`NEXT_PHASE_PLAN.md` Phase 8b）。

🔴 ただし **SHAP の内訳から「AIを信じてよいか」は読めない**（3窓26,340頭で -0.0038・
2026-08-27）。**説明の道具であって、選別の道具ではない。**

---

## 9. この棚卸しから言えること（提案ではなく事実）

1. **市場は既に特徴量から分離できている**（今回の市場に依存する列 0）。指示書の
   「馬能力DBとコースDBを別管理」は、市場については既に達成済み。
2. **`f_course_*` は3種類の混合だった**。指示書の指摘は正しく、(a)実績 / (b)条件 / (c)掛け算に
   分離した。ただし(c)の形は**既に存在し、2段階分離は 2026-08-04 に否定済み**。
3. **能力軸は1軸ではない**（PCA 第1主成分 39%）。分解する余地はデータ側にある。
   ただし軸内の重複が大きく、まず整理が先。
4. **死んでいる列が4つある。ただし本当に情報が無いのは `f_early_speed`（定数36.0）だけ**で、
   残り3つは別の列と同値だった（Phase 1b・§10）。**重複を消しても情報は減らない。**
5. 🔴 **最大の制約はデータで、設計ではない。** 馬場・馬体重・調教・コメントが
   いずれも 0〜6.5% しか埋まっていない。**新しい軸を設計しても入れる数字が無い。**

---

## 10. Phase 1b — 全学習データでの棚卸し（2026-09-16）

§0〜§9 は**本番の1レースを実データで回して入力を摂動**した結果。
Phase 1b は同じ問いを **`build_training_data()` の全出力**で測り直したもの。

```
horse_features.csv   164,915行 × 141列   （生成 8分28秒・隔離ワークスペース）
うち本番の特徴量      134列（CSVのみの7列は race_id / date / horse_name /
                        horse_num / place / is_fukusho / f_popularity）
```
🔑 **`f_popularity` はCSVに存在するが134列に入っていない。**
残差学習では市場が `base_margin` にしか入らない、という §1 の結論が全データで再確認された。
**本番モデルには一切触れていない**（scratch の base_dir で生成・シンボリックリンクなし）。

### (1) 死んだ4列 → **本当に情報が無いのは1列**
上の「分割に一度も出なかった4列」の表を参照。
`f_same_course_rate` は `f_course_fukusho` と**164,915行すべてでビット単位一致**、
`f_course_stamina_demand` は `f_course_hill_diff` の **1/4.3 倍**、
`f_is_debut` は `f_career_runs == 0` と**完全同値**だった。

### (2) 重複列（|r| >= 0.90）— **22対**
| r | 列 | gain |
|---|---|---|
| **+1.000** | `f_course_fukusho` × `f_same_course_rate` | 0.39% / **0.00%** |
| **+1.000** | `f_course_hill_diff` × `f_course_stamina_demand` | 0.62% / **0.00%** |
| +0.998 | `f_jockey` × `f_jockey_rate` | 1.14% / 0.84% |
| +0.982 | `cl_f_pace_fit_rank` × `cl_f_style_total_fit_rank` | 0.45% / 0.68% |
| +0.965 | `rl_f_pl_rating` × `rl_f_pl_rating_z` | 2.18% / 1.38% |
| +0.946 | `f_competitiveness` × `f_competitive_best` | 0.93% / 0.72% |
| -0.918 | `f_pace_prob_fast` × `f_pace_prob_slow` | 0.55% / 0.45% |
| +0.907 | `f_speed_fig_avg` × `f_speed_fig_max` | 0.66% / 0.99% |

⚠ **`x` と `cl_x_vs_field` の対が8対ある**（jockey / trainer / dist_fukusho /
course_fukusho / heavy_track / style_course_fit ほか）。レース内で相対化しても
**元の値とほぼ同じ順序**になる＝相対化が効いていない組がある、ということ。

### (3) 同じ意味の群（|r| >= 0.80 の連結成分）— **24群 / 75列 / gain 59.7%**
| gain | 列数 | 中身 |
|---|---|---|
| **16.19%** | 8 | `f_pl_rating` / `rl_f_pl_rating*` / `f_recent` / `rl_f_recent_*` / `f_last1_rank` |
| 4.22% | 4 | 騎手（`f_jockey` / `f_jockey_rate` / `cl_f_jockey_*`） |
| 3.93% | 2 | 過去の市場評価（`f_pop_avg` / `f_pop_last`） |
| 3.55% | 6 | ペース適合（`f_pace_fit` / `f_style_*_fit` / `cl_*_vs_field`） |
| 2.83% | 5 | スピード指数系（`f_speed_avg/last/max` ほか） |
| 2.77% | 5 | 距離適性（`f_dist_fukusho` / `f_recent_fukusho` ほか） |

🔑 **最大の塊（gain 16.19%）は「PLレーティング」と「近走成績」が
同じ群に落ちている。** §6 の能力軸 A（基礎的な競走能力）の候補はこの1塊で、
**8列がほぼ同じことを言っている**。Phase 2 が分解する対象はここ。

### (4) 能力軸候補（摂動分類・§1と同じ区分を全データに適用）
```
馬のみ          79列  gain 62.5%
馬×今回の条件    45列  gain 33.2%
レース共通       10列  gain  4.3%
```

### (5) 市場情報との関係
`f_popularity`（今回の人気）との相関。**134列の中で最大は +0.660**。

| | 列数 | gain合計 |
|---|---|---|
| \|r\| >= 0.5（市場と情報が重なる） | 13 | 27.7% |
| \|r\| < 0.3（市場とほぼ無関係） | 88 | **49.9%** |

上位は `rl_f_recent_rank` +0.660 / `f_pop_last` +0.625 / `rl_f_pl_rating_rank` +0.613 /
`f_cl_rank` +0.584。**近走成績と実力レーティングが市場と最も重なる**——
2026-08-28 R1 の「前走成績は市場が最も正確に織り込む情報」と一致する。

⚠ **これは相関であって情報の重複そのものではない。**
市場は `base_margin` から入るので、この表は目安にとどめる。
「市場と重ならない列が gain の半分」を**エッジの証拠として使わない**。
